# 4 系统设计

## 4.1 总体架构设计

结合项目代码结构，系统可以概括为前端展示层、接口服务层、业务编排层、检索与语义处理层以及数据存储层五个部分。

前端展示层由 Vue 3 页面组成，负责图片知识库、文档知识库、检索、聊天和系统设置等交互界面。接口服务层由 FastAPI 提供，对外暴露上传、检索、问答、进度查询和配置管理接口。业务编排层负责组织知识入库、文档解析、会话管理和状态同步等流程。检索与语义处理层是系统核心，承担语义描述生成、向量索引、BM25 检索、融合、重排和问答上下文组织等工作。数据存储层则同时维护关系型元数据、向量索引和本地文件。

这样的架构并不是为了形式上的分层，而是由当前项目的处理特点决定的。前端需要持续展示上传状态、解析进度和问答结果，后端则要同时处理文件落盘、数据库写入、向量检索和模型调用，如果界面逻辑与后端流程混杂在一起，后续很难同时维护交互和检索链路。另一方面，图片与文档的入库细节虽然不同，但它们最终都要进入检索和问答环节，因此系统在架构上需要保留一条可复用的统一主链路，而不是为每类数据维护完全割裂的实现。

## 4.2 核心模块设计

### 4.2.1 图片知识库模块

图片知识库模块负责完成图片的接收、存储、语义化和索引构建。上传图片后，系统首先保存文件并生成唯一标识，然后调用多模态模型生成图像语义描述。该描述既写入元数据，也作为后续向量化和文本检索的基础。完成向量库写入后，系统还会增量更新 BM25 侧索引。

这种设计使图片知识库不再只是一个文件列表，而是一个可被文本问题访问的语义化知识单元。用户后续无论是做文本搜图，还是在聊天中发起与资料相关的问题，都可以重用这条语义链路。

### 4.2.2 文档知识库模块

文档知识库模块面向 PDF 文档。由于文档解析通常比图片上传更耗时，系统采用“上传记录创建在前，后台解析在后”的方式组织流程。上传后，系统先创建文档记录并返回状态；随后后台任务负责抽取文本、切分文本块、提取图像和表格，并把结果同步到文档知识库。

这一设计首先是为了避免把长耗时解析放在单次上传请求里。文档上传后的文本抽取、图片裁剪、表格提取和索引写入都可能持续较长时间，如果坚持同步处理，接口更容易超时，前端也无法及时显示处理中状态。当前代码采用“先建记录、再后台解析、前端轮询进度”的方式，使文档在上传后就进入可管理状态，同时把真正耗时的解析过程放到后台完成，这比同步阻塞更符合系统实际使用方式。

### 4.2.3 检索模块

检索模块是系统的关键设计之一。它不是单一召回器，而是由查询改写、向量检索、BM25 检索、倒数排序融合和重排构成的多阶段流程。文本查询直接进入该流程；图像查询则先转为语义描述，再复用文本检索路径。

其中，语义描述桥接的作用主要体现在两个方面。其一，它让图像查询能够转换为统一的文本表示，从而直接复用向量检索、BM25、融合和重排这条文本主链路，而不必再为图像额外维护一套独立排序流程。其二，它让图片知识库与文档知识库在索引组织上采用相近口径，后续无论是做来源回溯、结果展示还是问答上下文拼接，都可以沿用更一致的数据结构。这一设计并不意味着图像原生向量没有价值，而是说明在当前系统里，桥接表示更便于把检索和问答环节真正接通。

### 4.2.4 会话问答模块

会话问答模块在检索模块之上进一步组织上下文选择和答案生成。系统不仅存储会话与消息，还会在问答过程中区分不同执行模式。例如，某些请求适合直接由大模型回答，某些请求则需要基于检索结果生成回答，某些请求则属于上传图片后的图像问答或相似检索。

这种设计体现出系统并未将所有输入都简单归并为一种固定流水线，而是根据输入类型和场景做差异化处理。当前聊天接口既要处理纯文本问题，也要处理带图提问、相似图检索和知识库问答，如果所有请求都强行经过同一条 RAG 流程，不仅会增加不必要的检索开销，还会让部分本应直接回答或直接检索的请求被拉长。因此，多执行模式分流并不是额外装饰，而是为了让不同交互入口进入更合适的处理路径。

### 4.2.5 管理与配置模块

管理与配置模块主要负责 BM25 索引重建、配置读取和配置更新。虽然该模块在界面上不如检索和问答直观，但它对系统稳定运行非常重要。特别是在知识库发生批量变动或参数调整时，提供可显式触发的管理能力，有助于减少隐式状态不一致带来的问题。

## 4.3 数据存储设计

当前系统至少包含三类存储对象：关系型元数据、向量索引和本地文件。关系型元数据主要保存图片、文档和会话等业务记录；向量索引用于语义检索；本地文件则保存原始图片、上传文档及其解析产生的中间结果。

这三类存储之间通过统一标识关联。图片和文档记录都带有唯一 ID，删除或更新时需要同时影响数据库记录、向量索引和文件系统内容。之所以要在设计阶段强调三存储一致性，是因为当前系统既有管理页面，又有检索与问答链路，一旦出现记录存在但文件缺失，或者文件存在但向量索引未同步，前端展示和后端检索都会出现难以定位的问题。代码中专门为删除、重处理和状态同步保留一致性控制逻辑，说明该问题在系统设计中是显式考虑的，而不是事后补丁。

从论文写作角度看，这一设计既能解释为什么系统需要数据库和向量库并存，也能说明为什么在实现章节中会出现与删除、重处理和状态同步相关的逻辑。

## 4.4 关键流程设计

### 4.4.1 图片入库流程

图片入库流程可概括为以下几个步骤：

1. 前端上传图片，请求发送到图片知识库接口。
2. 后端保存原始文件并生成唯一标识。
3. 系统调用多模态模型为图片生成语义描述。
4. 将图片元数据写入数据库，并将语义描述写入向量索引。
5. 增量更新 BM25 检索侧的数据。
6. 返回上传结果，前端刷新图片列表。

该流程体现了“先语义化，再统一入库”的设计思想。对当前项目而言，生成描述不是附加信息，而是后续检索能力的前提。

### 4.4.2 文档入库流程

文档入库流程与图片入库不同，主要差异在于其后台解析机制。

1. 用户上传 PDF，系统先创建文档记录。
2. 后台任务执行文本抽取和句子感知分块。
3. 后台任务提取页面图像、表格裁剪结果及必要的整页渲染图。
4. 将文档分块和视觉内容同步到对应知识库。
5. 更新解析阶段、进度百分比和状态信息。
6. 前端通过进度接口获取当前处理结果。

这一流程说明，文档知识库的设计重点不只是“保存 PDF 文件”，而是把文档中的多种可用信息整理为可检索、可问答的知识单元。

### 4.4.3 检索与问答流程

当用户发起文本检索或基于资料的问答时，系统会先对输入进行分析。对于适合检索的问题，流程一般包括查询改写、双路召回、结果融合和重排。若用户进入会话问答场景，系统会在检索结果基础上构建提示上下文，并保留来源信息和检索步骤供前端展示。

对于图像输入，系统会先生成图像描述，再把描述送入统一的检索链路。这样的设计减少了为图像检索和文本检索维护两套完全独立流程的成本，也让来源组织、候选排序和问答上下文构建可以复用同一套中间表示，更符合本项目“桥接”的总体思路。

## 4.5 前后端交互设计

从当前前端路由与页面实现来看，系统采用较典型的前后端分离模式。前端负责用户输入、列表展示、状态提示和结果可视化；后端负责文件处理、知识库维护、检索和模型调用。两端之间通过 HTTP 接口通信。

这种交互模式有利于毕业设计中的系统展示和后续维护。一方面，前端页面能够比较直观地体现图片管理、文档解析和问答流程；另一方面，后端逻辑集中在接口与应用层，便于进一步增加接口或替换底层模型配置。

## 4.6 本章小结

本章结合真实代码结构说明了系统总体架构、核心模块、数据存储方式和关键流程。可以看到，系统设计围绕一个明确目标展开，即把图片与文档两类资料统一纳入可语义化、可检索、可问答的知识服务流程中。下一章将在此基础上进一步说明各个模块的具体实现。
