# 基于语义描述桥接的多模态 RAG 知识库系统设计报告

## 1. 引言

### 1.1 项目背景

随着图像、图表、页面截图、文档版面等多模态数据在互联网和专业业务场景中的广泛积累，传统仅依赖关键词匹配或低层视觉特征的检索方式已难以满足高质量知识服务需求。尤其是在问答场景中，如果系统无法有效理解图像内容或文档中的视觉结构，生成模型便缺少可靠的外部上下文支撑，容易出现语义偏差、事实缺失和答案不可解释等问题。

检索增强生成技术已经在纯文本问答中取得良好效果，但直接将传统文本 RAG 迁移到多模态场景仍存在明显局限。一类方案依赖统一跨模态向量空间进行图文对齐，工程复杂度较高，且在垂直领域中对数据质量和模型适配要求较强；另一类方案则尝试将图像和文档中的视觉信息转换为文本，再借助成熟的文本检索与问答链路完成知识服务。

基于上述背景，本系统采用“语义描述桥接”的总体思路，即通过多模态大模型将图像内容转换为结构化文本描述，将复杂的多模态检索问题转化为统一的文本语义检索问题，并在此基础上构建面向图片知识库、文档知识库、双路检索和多模态问答的一体化系统。

### 1.2 建设目标

本系统拟实现以下目标：

1. 建立统一的多模态知识管理平台，支持图片和文档两类知识源的入库、维护和检索。
2. 支持文本到图像、图像到图像两类核心检索路径，并具备良好的可扩展性。
3. 构建基于检索增强生成的多模态智能问答能力，实现图文结合的知识服务。
4. 提供会话管理、历史消息、引用来源和检索轨迹等机制，提升系统可用性与可解释性。
5. 建立离线评测体系，为后续论文实验和方案对比提供支撑。

## 2. 系统总体设计

### 2.1 总体架构

系统总体采用五层架构，自下而上分为数据层、语义化层、检索层、应用层和展示层。

#### 2.1.1 数据层

数据层负责原始资源、业务元数据和向量索引的统一存储。图片原文件、PDF 文档和文档衍生图像资源采用文件系统保存；结构化业务数据采用关系型数据库管理；语义向量索引采用向量数据库保存；关键词召回索引采用本地 BM25 索引保存。

#### 2.1.2 语义化层

语义化层负责将图像内容或文档中的视觉信息转换为可检索的结构化文本描述。对于图片知识库，该层通过多模态大模型生成覆盖主体对象、属性特征、空间关系和场景语义的描述文本；对于文档知识库，该层负责文本抽取、页面解析、视觉资产识别和必要的图像语义补全。

#### 2.1.3 检索层

检索层负责将图像描述、文档片段和用户查询统一编码为文本语义向量，并完成向量召回、BM25 召回、候选融合和重排序。该层通过统一的检索接口向上层提供文本到图像、图像到图像和文档片段召回能力。

#### 2.1.4 应用层

应用层负责业务编排，包括知识入库、双路检索调度、RAG 问答流程控制、会话管理、消息持久化、来源回溯和检索轨迹记录。该层是系统中连接检索能力与用户交互能力的核心层。

#### 2.1.5 展示层

展示层通过 Web 前端为用户提供统一入口，支持图片和文档上传、知识库管理、文本检索、图像检索、多轮问答和结果可视化展示。

### 2.2 系统核心思想

1. 以结构化文本描述作为多模态知识的统一语义中间层。
2. 以统一文本检索链路承载图片检索和文档检索能力。
3. 以检索增强生成为问答核心流程，降低生成模型幻觉。
4. 以来源追溯和过程可视化增强系统可信性。
5. 以模块化和配置化方式支持模型、向量库和解析后端替换。

### 2.3 总体数据流

系统的总体数据流如下：

1. 用户上传图片或文档。
2. 系统对图片生成语义描述，对文档进行解析和切块。
3. 系统将描述文本和文档片段写入向量索引，同时在关系型数据库中登记元数据。
4. 用户提交文本问题或图片问题。
5. 系统统一将查询映射到文本检索通路，完成候选召回。
6. 系统根据检索结果构建上下文，由生成模型输出答案。
7. 系统返回检索结果、引用来源和检索轨迹，并持久化消息记录。

## 3. 功能设计

### 3.1 图片知识库功能设计

图片知识库面向图像类知识资源，主要设计如下：

1. 支持单张或多张图片上传。
2. 上传后自动生成结构化语义描述。
3. 为每张图片建立唯一标识及元数据记录。
4. 支持图片状态管理，包括处理中、完成和失败。
5. 支持图片标题、来源数据集、标签、备注、自定义元数据维护。
6. 支持图片列表查询、关键字筛选、标签筛选、启用状态筛选。
7. 支持对图片重新处理，重新生成描述并同步更新检索索引。
8. 支持图片删除，并同步清理相关检索索引。

### 3.2 文档知识库功能设计

文档知识库面向 PDF 等文档型知识资源，主要设计如下：

1. 支持 PDF 文档上传。
2. 支持后台异步解析文档内容。
3. 支持文档文本抽取、分块、页面图像提取和表格图像处理。
4. 支持展示文档解析进度，包括阶段、进度百分比和提示信息。
5. 支持查看文档解析结果，包括文本片段和关联图像资产。
6. 支持文档标题、类型、标签、备注、启用状态和自定义元数据维护。
7. 支持文档重新解析和删除。

### 3.3 双路检索功能设计

系统采用统一检索框架实现两条主要检索路径：

#### 3.3.1 文本到图像检索

用户输入自然语言查询后，系统直接对文本进行向量化，在图片描述索引和相关知识集合中进行相似度检索，返回相关图片结果。

#### 3.3.2 图像到图像检索

用户上传查询图片后，系统先生成该图片的结构化文本描述，再将该描述作为检索查询送入统一文本检索流程，返回语义相近的图片结果。

### 3.4 多模态 RAG 问答功能设计

多模态问答模块主要设计如下：

1. 支持用户输入文本问题。
2. 支持在提问时附加图片作为补充语义输入。
3. 根据问题和附件执行检索，召回相关图片描述、文档片段和视觉证据。
4. 使用检索结果构建上下文 Prompt，交由生成模型输出答案。
5. 返回回答内容的同时返回引用来源。
6. 支持记录和展示检索轨迹，增强问答解释能力。

### 3.5 会话与历史管理功能设计

为了支持连续问答和系统可用性，聊天模块设计如下：

1. 支持新建会话。
2. 支持查看会话列表。
3. 支持查看某个会话的历史消息。
4. 支持会话重命名和删除。
5. 支持保存用户消息和助手消息。
6. 支持为消息保存来源快照和检索参数。
7. 支持根据最近若干轮历史消息构建上下文记忆。

### 3.6 离线评测功能设计

系统需配套离线评测能力，用于论文实验与方案验证，主要设计如下：

1. 支持加载公开数据集和领域数据集。
2. 支持 Proposed 方法和多种 Baseline 方法的统一评测。
3. 支持计算 Recall@K、MRR、mAP 等检索指标。
4. 支持输出实验结果文件，便于后续统计分析和论文写作。

## 4. 模块设计

### 4.1 后端模块设计

后端采用 FastAPI 作为统一服务框架，模块设计如下。

#### 4.1.1 API 路由模块

API 路由模块负责暴露系统接口，主要包括：

1. 图片知识库接口模块。
2. 文档知识库接口模块。
3. 检索接口模块。
4. 聊天与会话接口模块。
5. 管理类辅助接口模块。

#### 4.1.2 应用服务模块

应用服务模块负责系统业务编排，主要包括：

1. 知识库管理服务。
2. 聊天会话服务。
3. 请求与响应 Schema 定义。
4. 数据字段编解码和业务校验工具。

#### 4.1.3 数据模块

数据模块负责关系型数据建模和文件存储路径管理，主要包括：

1. 图片记录模型。
2. 文档记录模型。
3. 会话模型。
4. 消息模型。
5. 数据库连接和会话工厂。
6. 文件路径和存储目录管理。

#### 4.1.4 语义与检索集成模块

该模块负责连接多模态模型、嵌入模型、向量库和 RAG 链路，主要包括：

1. 图片描述生成链。
2. 统一检索器。
3. 向量库操作模块。
4. 文档解析模块。
5. 查询改写、上下文压缩和增强检索模块。
6. RAG 问答链和适配层。

#### 4.1.5 检索增强模块

检索增强模块主要负责：

1. 混合检索。
2. 重排序。
3. 查询扩展。
4. 候选过滤与阈值控制。

### 4.2 前端模块设计

前端采用 Vue3 + TypeScript + Vite 技术栈，主要模块如下：

#### 4.2.1 页面模块

1. 图片知识库页面。
2. 文档知识库页面。
3. 双路检索页面。
4. 聊天问答页面。

#### 4.2.2 公共组件模块

1. 上传区域组件。
2. 图片卡片组件。
3. 图片预览组件。
4. 会话列表组件。

#### 4.2.3 API 通信模块

前端通过统一的 HTTP 封装与后端接口通信，并按功能划分为图片知识库 API、文档知识库 API、检索 API 和聊天 API。

#### 4.2.4 路由与状态模块

前端通过路由管理页面跳转，并在页面内部维护上传状态、检索状态、消息状态和会话状态。

### 4.3 评测模块设计

评测模块独立于在线服务，主要包括：

1. 数据集加载模块。
2. 方法实现模块。
3. 指标计算模块。
4. 生成式评估模块。
5. 实验执行脚本模块。

## 5. 数据库设计

### 5.1 数据存储总体设计

系统采用“关系型数据库 + 向量数据库 + 文件存储”的复合存储方案。

1. 关系型数据库负责保存图片、文档、会话和消息等业务主数据。
2. 向量数据库负责保存图片描述和文档片段的语义向量。
3. 文件存储负责保存图片原文件、PDF 文档和文档解析产生的衍生图像资源。

### 5.2 关系型数据库表设计

#### 5.2.1 图像记录表 `image_records`

该表用于保存图片知识库中的图片元数据。

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | String | 主键，图片唯一标识 |
| file_path | String | 图片存储路径 |
| upload_time | DateTime | 上传时间 |
| generated_description | Text | 生成的结构化描述 |
| status | String | 图片处理状态 |
| source_dataset | String | 来源数据集 |
| title | String | 图片标题 |
| tags | String | 标签信息 |
| notes | Text | 管理备注 |
| enabled | Boolean | 是否启用 |
| custom_metadata | Text | 自定义元数据 JSON |
| extra_metadata | Text | 扩展元数据 JSON |

#### 5.2.2 文档记录表 `document_records`

该表用于保存文档元数据及解析过程信息。

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | String | 主键，文档唯一标识 |
| file_name | String | 原始文件名 |
| title | String | 文档标题 |
| file_path | String | 文档存储路径 |
| upload_time | DateTime | 上传时间 |
| status | String | 文档处理状态 |
| chunk_count | Integer | 文本片段数量 |
| image_count | Integer | 提取图像数量 |
| document_type | String | 文档类型 |
| tags | String | 标签信息 |
| notes | Text | 管理备注 |
| enabled | Boolean | 是否启用 |
| custom_metadata | Text | 自定义元数据 JSON |
| extra_metadata | Text | 扩展元数据 JSON |
| parse_backend | String | 解析后端类型 |
| parse_stage | String | 解析阶段 |
| progress_percent | Integer | 解析进度百分比 |
| progress_message | Text | 进度提示 |

#### 5.2.3 会话表 `chat_sessions`

该表用于保存聊天会话信息。

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | String | 主键，会话唯一标识 |
| title | String | 会话标题 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 最后更新时间 |

#### 5.2.4 消息表 `chat_messages`

该表用于保存会话中的每条消息及其检索痕迹。

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| id | Integer | 主键，自增 |
| session_id | String | 所属会话 ID |
| role | String | 消息角色 |
| content | Text | 消息内容 |
| has_image | Boolean | 是否附带图片 |
| sources_json | Text | 来源快照 JSON |
| retrieval_params_json | Text | 检索参数 JSON |
| retrieval_steps_json | Text | 检索步骤 JSON |
| created_at | DateTime | 创建时间 |

### 5.3 向量库设计

系统向量数据库设计如下：

1. 图片语义向量集合：保存图片描述文本的向量表示。
2. 文档文本向量集合：保存文档切块文本的向量表示。
3. 实验评测集合：用于隔离评测数据与在线系统数据。

向量记录中除保存文本内容外，还应保存如下 metadata：

1. 资源 ID。
2. 文件路径。
3. 资源类型。
4. 页面号或块编号。
5. 标题和来源数据集。
6. 启用状态。

### 5.4 文件存储设计

文件存储设计如下：

1. 图片资源保存在图片存储目录。
2. 文档原文件保存在文档存储目录。
3. 文档解析过程中产生的页面渲染图、表格截图等衍生资源保存在专用目录。
4. 文件路径通过数据库字段与向量 metadata 建立关联。

### 5.5 实体关系设计

系统中的主要实体关系如下：

1. 一个会话对应多条消息。
2. 一个文档对应多个文本片段。
3. 一个文档对应多个图像资产。
4. 一条图片记录对应一条主描述向量。
5. 一条消息可关联多个引用来源。

## 6. 关键流程设计

### 6.1 图片上传与入库流程

1. 用户上传图片。
2. 系统保存图片原文件并创建图片记录。
3. 系统调用多模态模型生成结构化描述。
4. 系统更新图片记录状态与描述文本。
5. 系统将描述写入向量集合。
6. 系统返回入库结果。

### 6.2 文本到图像检索流程

1. 用户输入文本查询。
2. 系统将查询文本编码为向量。
3. 系统在图片向量集合中召回候选结果。
4. 系统根据需要执行混合检索和重排序。
5. 系统返回相关图片列表及相似度分数。

### 6.3 图像到图像检索流程

1. 用户上传查询图片。
2. 系统生成查询图片的结构化描述。
3. 系统将描述文本作为查询执行统一检索。
4. 系统返回查询描述及相似图片结果。

### 6.4 文档上传与解析流程

1. 用户上传 PDF 文档。
2. 系统创建文档记录并进入解析队列。
3. 系统执行文本抽取和切块处理。
4. 系统提取文档中的页面图像或表格图像。
5. 系统将文本片段写入文档向量集合。
6. 系统更新文档状态、进度和统计信息。
7. 用户可查看解析结果。

### 6.5 会话化问答流程

1. 用户在聊天界面发起问题。
2. 系统识别是否包含图片输入。
3. 系统读取会话历史作为上下文。
4. 系统执行检索并获取相关知识片段。
5. 系统构建 RAG Prompt 并调用生成模型。
6. 系统保存消息、来源和检索轨迹。
7. 系统返回答案及引用来源。

## 7. 接口设计

### 7.1 图片知识库接口

1. `POST /api/knowledge-base/upload`
2. `GET /api/knowledge-base/list`
3. `GET /api/knowledge-base/{image_id}`
4. `PATCH /api/knowledge-base/{image_id}`
5. `DELETE /api/knowledge-base/{image_id}`
6. `POST /api/knowledge-base/{image_id}/reprocess`

### 7.2 文档知识库接口

1. `POST /api/docs/upload`
2. `GET /api/docs/list`
3. `GET /api/docs/{doc_id}`
4. `PATCH /api/docs/{doc_id}`
5. `DELETE /api/docs/{doc_id}`
6. `POST /api/docs/{doc_id}/reprocess`
7. `GET /api/docs/{doc_id}/progress`
8. `GET /api/docs/{doc_id}/result`

### 7.3 检索接口

1. `POST /api/search/text-to-image`
2. `POST /api/search/image-to-image`

### 7.4 聊天接口

1. `POST /api/chat/sessions`
2. `GET /api/chat/sessions`
3. `GET /api/chat/sessions/{session_id}`
4. `PATCH /api/chat/sessions/{session_id}`
5. `DELETE /api/chat/sessions/{session_id}`
6. `POST /api/rag/chat`
7. `POST /api/rag/chat/stream`

## 8. 非功能设计

### 8.1 性能设计

1. 采用向量检索与文本切块控制上下文规模。
2. 采用候选集控制、重排序和上下文压缩优化响应效率。
3. 采用异步后台任务处理文档解析，避免阻塞上传流程。

### 8.2 可扩展性设计

1. 多模态模型、嵌入模型和生成模型采用配置化接入。
2. 支持图片知识库和文档知识库统一扩展。
3. 支持后续增加更多解析后端、更多检索策略和更多评测方法。

### 8.3 可解释性设计

1. 检索结果需返回相似度分数和来源路径。
2. 问答结果需返回引用来源。
3. 系统需保留检索轨迹信息，便于分析和展示。

### 8.4 可维护性设计

1. 采用分层架构，降低模块耦合。
2. 路由层、应用层、数据层和模型集成层职责明确。
3. 前后端接口通过统一数据结构约束，降低联调复杂度。

### 8.5 安全与配置设计

1. 模型 API Key 和数据库配置通过环境变量管理。
2. 开发环境采用本地文件存储和轻量级数据库，便于部署和调试。
3. 删除类操作需有显式确认机制。

## 9. 技术选型

### 9.1 后端技术选型

1. Python 3.10+
2. FastAPI
3. SQLAlchemy
4. SQLite

### 9.2 检索与模型技术选型

1. ChromaDB 作为向量数据库。
2. 文本嵌入模型用于描述文本和文档片段编码。
3. 多模态大模型用于图片描述生成和视觉语义理解。
4. 大语言模型用于问答生成。
5. LangChain 用于统一组织描述生成、检索和 RAG 链路。

### 9.3 前端技术选型

1. Vue 3
2. TypeScript
3. Vite
4. Element Plus

## 10. 测试与评估设计

### 10.1 功能测试

1. 图片上传与描述生成测试。
2. 文档上传与解析流程测试。
3. 文本检索与图像检索测试。
4. 聊天会话和历史消息测试。
5. 问答来源回溯测试。

### 10.2 性能评估

1. 检索响应时间评估。
2. 文档解析耗时评估。
3. 问答生成延迟评估。

### 10.3 离线实验评估

1. 基于公开数据集评估检索准确率。
2. 对 Proposed 方法与 Baseline 方法进行对比。
3. 使用 Recall@K、MRR、mAP 等指标分析系统效果。

## 11. 结论

本系统围绕“语义描述桥接”的设计思想，构建了一个面向图片知识库、文档知识库、双路检索和多模态问答的一体化平台。系统在总体架构上采用分层设计，在知识表达上采用统一文本语义中间层，在业务能力上覆盖知识入库、检索、会话和问答，在实验层面具备离线评测能力，具有较好的工程可实施性与研究价值。

该设计既满足毕业设计对系统完整性的要求，也为后续实现、测试、优化和论文撰写奠定了明确基础。
