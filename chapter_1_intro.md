# 1 绪论

## 1.1 研究背景

在图像、扫描文档和多模态资料快速积累的场景中，传统知识库系统主要依赖结构化文本或纯文本索引。对于图片类资料，这种方式往往需要人工补录说明；对于 PDF 文档，尤其是版式复杂、含图表和跨页内容的文档，仅依靠 OCR 文本又容易丢失版面语义和视觉信息。随着检索增强生成与多模态大模型的发展，如何让外部知识更稳定地参与多模态问答，已经成为图文检索与智能问答系统共同面对的问题[1-7]。

目前较常见的工程路线主要有两类。一类做法是直接对图像建立多模态向量索引，通过跨模态嵌入完成召回；另一类做法是先进行 OCR 或文档解析，再将提取得到的文本送入传统文本检索流程[2,7-14]。这两种路线各有优点，但在当前项目面对的资料管理场景下也存在明显局限。直接做图像向量检索在开放场景中较为直接，但面对文档图像、页面片段和包含复杂版式的材料时，检索结果不总是容易解释；只依赖 OCR 文本又受识别质量和版面信息损失的影响，在图像信息较强、文本信息较弱的样本上表现不稳定。

本项目的出发点是，在不放弃多模态信息的前提下，为图像和文档构建一条更容易统一到检索与问答流程中的语义化通路。具体做法是利用多模态大模型为图像生成语义描述，再将该描述作为检索和问答链路中的桥接表示，与文本检索、向量检索和问答生成过程衔接起来。这样既保留了图像内容的语义信息，又使后续的索引、召回、融合和重排能够沿用较成熟的文本处理链路，也更契合近年来多模态视觉语言模型与多模态 RAG 系统的发展方向[5-9,15-18]。

## 1.2 课题来源与研究意义

本课题来源于多模态知识管理与智能问答的工程实践需求。结合当前项目的实现可以看到，系统不仅需要完成图片的上传和管理，还需要支持文本搜图、图搜图、基于资料来源的问答、文档上传解析和检索结果回溯。若系统只能完成单一检索功能，用户仍然需要在不同工具之间来回切换，无法形成完整的知识使用闭环。

从工程角度看，围绕“语义描述桥接”设计一套可运行的多模态 RAG 系统，至少有三方面意义。其一，能够将图片和文档中的非结构化信息纳入统一的知识库管理流程，降低资料散落存储带来的使用成本。其二，通过混合检索、重排和会话化问答，可以把“能检索到”进一步推进到“能给出带来源依据的回答”。其三，系统同时保留了离线评测链路，便于对不同方法进行横向比较，这一点对于毕业设计中的结果分析尤为重要。

需要说明的是，本文并不把该系统包装为通用研究平台，而是将其定位为一个围绕真实项目实现形成的本科工程型系统。论文中的设计选择、实现范围和实验分析都以项目现有代码、脚本和结果文件为依据，不超出当前工程条件去夸大结论。

## 1.3 国内外相关工作概述

与本课题直接相关的工作主要集中在三个方向。第一类是面向图文数据的多模态表示学习与跨模态检索方法，其代表思路是将图像和文本映射到同一向量空间，通过相似度实现召回，这一方向的代表性工作包括 CLIP、Flamingo、BLIP-2、LLaVA、InstructBLIP 以及 Qwen-VL、Qwen2-VL 等模型[2,8-9,15-18]。第二类是围绕 OCR、版面解析和文档理解构建的文档检索与问答方法，常见思路包括基于 OCR 的文本抽取、基于布局建模的文档理解以及 OCR-free 的文档理解模型[10-14]。第三类是以检索增强生成为核心的问答系统，通过先检索、再生成的方式提高答案与知识库内容的一致性，近年来又进一步发展为多模态检索增强生成研究[1,5-7,19-20]。

从现有工程经验看，单一路线往往难以覆盖本项目中的全部场景。直接使用多模态嵌入适合构建图文统一索引，但对复杂文档页面的解释性支持有限；只使用 OCR 文本可以接入成熟的文本检索流程，但对图片主导的信息表达支持不足。本文采用的语义描述桥接方法，本质上是在两者之间寻求一个工程上的折中：先借助多模态模型将图像内容转写为较完整的语义描述，再通过文本侧索引和后续混合检索链路完成召回与问答。这样的路线既吸收了多模态模型在视觉理解上的优势，也延续了文本检索和 RAG 在工程实践中的可复用能力[1-2,5-9,15-18,21-23]。

## 1.4 本文主要工作

围绕上述问题，本文完成的主要工作如下。

1. 设计并实现了一套多模态 RAG 系统。系统后端基于 FastAPI 构建，前端基于 Vue 3 实现，形成了图片知识库、文档知识库、检索、会话问答和配置管理等完整模块。
2. 在知识入库侧实现了两类处理流程。对于图片资料，系统在上传后生成语义描述并建立向量索引；对于 PDF 文档，系统执行文本抽取、句子感知分块、页面图像与表格裁剪提取，并将解析结果同步到文档知识库。
3. 在检索与问答侧实现了多阶段链路。系统支持查询改写、向量检索、BM25 检索、倒数排序融合和重排，并在问答过程中保留检索轨迹和来源信息，便于结果解释。
4. 基于项目中的 `evaluation` 脚本和真实实验结果，对语义描述桥接方法、多模态向量基线和 OCR 文本基线进行了离线比较；同时结合已有生成实验和微调实验结果，对系统在不同场景下的效果进行了分析。

## 1.5 论文结构安排

全文共分为七章。

- 第1章介绍课题背景、研究意义、相关工作概况以及本文主要工作。
- 第2章介绍与本项目直接相关的关键技术，包括多模态语义描述、混合检索、RAG 和文档解析等内容。
- 第3章结合系统实际功能，对应用场景、功能需求和非功能需求进行分析。
- 第4章从系统架构、模块职责、数据组织和关键流程几个方面说明整体设计。
- 第5章依据实际代码实现，介绍图片知识库、文档知识库、检索和会话问答等核心模块的实现方式。
- 第6章依据实验脚本和结果文件，给出检索实验、生成实验和补充微调实验的设计与分析。
- 第7章总结全文工作，并结合当前实现讨论系统局限与后续改进方向。
