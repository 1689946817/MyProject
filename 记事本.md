## 清空数据库 & 重新上传图片流程

### 注意事项
- 清空脚本必须从**项目根目录**运行，不能在 backend/ 下运行
- 后端数据库实际位置是 `backend/app.db`（后端从 backend/ 启动，./app.db 指向那里）
- 上传脚本运行前必须先启动后端服务，否则接口不通
- `data/coco_id_to_uuid.json` 存在时脚本会断点续传，重新上传前需删除该文件

### 第一步：停止后端服务（Ctrl+C）

### 第二步：清空所有数据
```bash
# 在项目根目录执行
python scripts/clear_all_data.py
```
清空内容：SQLite image_records 表 + ChromaDB 三个集合 + backend/storage/images/ 图片文件

### 第三步：删除旧映射文件
```bash
del data\coco_id_to_uuid.json
```

### 第四步：重启后端
```bash
cd backend && python main.py
```

### 第五步：重新上传图片
```bash
# 回到项目根目录
cd ..

# 上传（--num-images 控制数量，测试用 10，正式用 100）
python scripts/upload_coco_subset.py \
  --captions-json data/annotations/captions_val2017.json \
  --auto-download \
  --download-dir data/val2017_subset \
  --num-images 100 \
  --output-mapping data/coco_id_to_uuid.json
```

### 第六步：生成评估数据集
```bash
python scripts/build_eval_dataset.py \
  --captions-json data/annotations/captions_val2017.json \
  --mapping data/coco_id_to_uuid.json \
  --output data/coco_subset_eval.json
```

### 第七步：构建 baseline 向量索引
```bash
python scripts/build_baseline_index.py --method all --mapping data/coco_id_to_uuid.json
```

### 第八步：运行检索评估
```bash
python -m evaluation.run_offline_eval --dataset-path data/coco_subset_eval.json --method proposed --top-k 10
python -m evaluation.run_offline_eval --dataset-path data/coco_subset_eval.json --method baseline_clip --top-k 10
python -m evaluation.run_offline_eval --dataset-path data/coco_subset_eval.json --method baseline_ocr --top-k 10
```

---

1.COCO数据集
2.专业垂直领域图像集：InfoSeek 或 VisDoM
3.关系与空间推理数据集：Winoground 或 VSR
4.多模态密集型图表与文档：ChartQA 或 DocVQA。
5.生成评估再加一个无rag对照组


# coco数据集生成评估使用（检索器）方法
完整执行流程

  第一步：下载 captions 标注文件（约 1MB）

# 在项目根目录创建 data 目录

  mkdir -p data/annotations

# 下载标注文件（只需这一个，不需要下载 6GB 图片包）

# 浏览器访问或用 curl/wget：

  http://images.cocodataset.org/annotations/annotations_trainval2017.zip

  解压后把 captions_val2017.json 放到 data/annotations/ 下。

------

  第二步：启动后端

  cd backend && python main.py

------

  第三步：自动下载 100 张图片并上传到知识库

  python scripts/upload_coco_subset.py \
    --captions-json data/annotations/captions_val2017.json \
    --auto-download \
    --download-dir data/val2017_subset \
    --num-images 100 \
    --output-mapping data/coco_id_to_uuid.json

  脚本会逐张从 COCO 官网下载图片（每张约几百 KB），下载后立即上传，支持断点续传。

------

  第四步：生成评估数据集

  python scripts/build_eval_dataset.py \
    --captions-json data/annotations/captions_val2017.json \
    --mapping data/coco_id_to_uuid.json \
    --output data/coco_subset_eval.json

------

  第五步：构建 baseline 向量索引

  python scripts/build_baseline_index.py \
    --method all \
    --mapping data/coco_id_to_uuid.json

------

  第六步：运行检索评估

  python -m evaluation.run_offline_eval \
    --dataset-path data/coco_subset_eval.json \
    --method proposed --top-k 10

  python -m evaluation.run_offline_eval \
    --dataset-path data/coco_subset_eval.json \
    --method baseline_clip --top-k 10

  python -m evaluation.run_offline_eval \
    --dataset-path data/coco_subset_eval.json \
    --method baseline_ocr --top-k 10

------

  按这个顺序走下来就能得到三种方法的 Recall@1/5/10、MRR、mAP@10 对比数据。第三步耗时最长（上传 + MLLM
  描述生成），其余步骤都很快。有问题随时告诉我。

iic/MTWI
iic/WebText_Dataset
liekkas/table_recognition
DatatangBeijing/9497Images-OCRDataof10TypesofForms
DatatangBeijing/20000ImageCaptionDataOfDiverseScenes
DatatangBeijing/20000ImageCaptionDataOfOCRInNaturalScenes
wangxingjun778/muge_test
Tongyi-DataEngine/SA1B-Dense-Caption
liekkas/table_recognition
DatatangBeijing/500000_Images_Natural_Scenes_Document_Photograph_Scenes_Electronic_Scenes_OCR_Data_of_21_Countries
DatatangBeijing/2858306Pairs_Image_Caption_Data_Of_General_Scenes






  结论

  这三组基线里，baseline_clip 最强，proposed 很接近，baseline_ocr 明显掉队。

  ---
  1. 整体效果对比

  baseline_clip

  综合指标最好：
  - Recall@1 = 0.7445
  - MRR = 0.9708
  - mAP@10 = 0.9570

  说明它在 “第一名就命中” 和 整体排序质量 上略优，更适合作为当前 UniDoc finance 场景下的最强检索基线。

  proposed

  表现非常接近，而且在更大召回范围上略好：
  - Recall@1 = 0.7045，略低于 clip
  - Recall@5 = 0.9742，略高于 clip 的 0.9722
  - Recall@10 = 0.9900，高于 clip 的 0.9833

  这说明 proposed 的特点是：
  - Top1 精度稍逊于 clip
  - 但 相关结果更容易出现在前 5/10 个里
  - 如果后续生成阶段会消费多张候选图，proposed 其实很有竞争力

  baseline_ocr

  效果明显不行：
  - Recall@1 = 0.0200
  - Recall@5 = 0.0450
  - Recall@10 = 0.0890
  - MRR / mAP 也都接近 0

  这基本说明：
  在这个 finance 子集上，单靠 OCR 文本做主检索几乎不可用。很多问题依赖版式、图表、视觉结构，而不是纯文字匹配。

  ---
  2. proposed 和 clip 的关系怎么理解

  这两个方法不是“一个远超另一个”，而是：

  - clip 更擅长把最相关结果直接排到第 1
  - proposed 更擅长保证前几名里能覆盖到正确答案

  所以如果你的论文/实验重点是：
  - 首位命中、排序质量 → clip 略优
  - 多候选召回、给下游生成更多可用上下文 → proposed 很有价值

  换句话说，proposed 不是失败，而是目前在这个数据集上 还没有全面超过 clip，但已经接近，而且在 top-k 覆盖上有自己的优势。

  ---
  3. 时延对比

  三者延迟其实差距不大：
  - proposed: 370.8 ms
  - baseline_clip: 353.4 ms
  - baseline_ocr: 386.2 ms

  说明：
  - clip 不仅效果最好，速度也略快
  - proposed 没有慢很多，代价是可以接受的
  - ocr 反而没带来任何速度优势，却牺牲了大量效果

  所以从“效果/效率比”看，ocr 是最不划算的。

  ---
  4. 对实验结论的启发

  如果写实验分析，可以这么落：

  1. finance 场景下视觉信息很关键
  clip 和 proposed 都远强于 ocr，说明文档图像的布局、图表、表格结构比纯 OCR 文本更重要。
  2. proposed 方法有效，但当前尚未全面超越 clip
  proposed 在 Recall@5/10 上更强，说明“先把图像转成语义描述再检索”确实能提升候选覆盖能力；
  但在 Recall@1/MRR/mAP 上略弱，说明其描述质量或向量表达还存在优化空间。
  3. OCR-only 不适合作为主方法
  可以保留作弱基线，但不适合当主检索方案。

  ---
  5. 一句话总结

  当前 UniDoc finance 结果表明：baseline_clip 是最强基线，proposed 非常接近且在 top-k 召回上更优，baseline_ocr
  基本失效。

  如果你愿意，我下一步可以直接帮你把这三组结果整理成一段适合写进论文“实验结果分析”小节的正式表述。










  python -m evaluation.run_unidoc_eval --domain finance --method proposed --top-k 10 --build-index --retry-failed




  根据这 3 个结果文件看，energy 领域的结论非常清楚：proposed ≈ baseline_clip >>> baseline_ocr。                                                            
                                                                                                                                                           
  对应文件：                                                                                                                                                 - data/eval_results/unidoc_energy_baseline_ocr_20260325_112424.json                                                                                      
  - data/eval_results/unidoc_energy_baseline_clip_20260325_112726.json                                                                                       - data/eval_results/unidoc_energy_proposed_20260325_204353.json                                                                                                                                                                                                                                                     
  1. 总体表现

  proposed

  - Recall@1: 0.7092
  - Recall@5: 0.9800
  - Recall@10: 0.9875
  - MRR: 0.9725
  - MAP@10: 0.9589
  - 平均延迟: 440 ms

  baseline_clip

  - Recall@1: 0.6892
  - Recall@5: 0.9425
  - Recall@10: 0.9800
  - MRR: 0.9617
  - MAP@10: 0.9367
  - 平均延迟: 276 ms

  baseline_ocr

  - Recall@1: 0.0100
  - Recall@5: 0.0283
  - Recall@10: 0.0633
  - MRR: 0.0297
  - MAP@10: 0.0311
  - 平均延迟: 377 ms

  2. 简单结论

  proposed 最好

  - 在 energy 上，proposed 的所有主要检索指标都最好。
  - 相比 baseline_clip，提升不算特别巨大，但比较稳定：
    - Recall@1 高 2 个点
    - Recall@5 高 3.75 个点
    - MAP@10 高 2.22 个点
  - 说明你这套“先生成描述再做文本检索”的方案，在 energy 这种图表、实验结果、论文页较多的场景里，确实比纯 CLIP 更容易抓住语义。

  baseline_clip 很强，且接近 proposed

  - baseline_clip 已经非常强，尤其：
    - Recall@10 达到 0.98
    - MRR 达到 0.9617
  - 这说明 energy 领域的很多查询，其实单靠图文同空间表示就能检到正确页。
  - 但它在 top-1 和整体排序质量上还是略输给 proposed，说明 proposed 在“把正确页排到更靠前位置”上更有优势。

  baseline_ocr 基本失效

  - baseline_ocr 在 energy 上几乎不可用：
    - Recall@1 只有 1%
    - Recall@10 只有 6.33%
  - 而且延迟并不低，甚至比 CLIP 还慢。
  - 这说明 energy 领域的问题并不主要依赖 OCR 文本匹配，更多依赖：
    - 图表结构
    - 实验曲线
    - 图注与视觉布局
    - 复合语义关系

  3. 分类型看

  按 question_type

  factual_retrieval

  - proposed: Recall@1 0.7799
  - clip: Recall@1 0.7286
  - ocr: Recall@1 0.0000

  factual_retrieval 上 proposed 明显最好，说明它对“找具体实验图/结果页”的能力更强。

  comparison

  - proposed: Recall@1 0.6711
  - clip: Recall@1 0.6447
  - ocr: Recall@1 0.0263

  comparison 也是 proposed 略优。这个很合理，因为比较类问题通常需要更强的语义概括，MLLM 生成描述更有帮助。

  causal_reasoning

  - proposed 和 clip 基本一样强：
    - Recall@5 / Recall@10 都接近或达到 1.0
  - ocr 还是很差

  这说明这类问题在 energy 上，视觉语义主导，OCR 帮助很有限。

  summarization

  - clip 的 Recall@1 (0.75) 略高于 proposed (0.65)
  - 但 proposed 的 Recall@5/10 是 1.0
  - 差距不大，整体上还是两者都很强

  temporal_comparison

  - 这个类别样本数只有 2
  - 结果波动大，参考价值有限
  - 但 proposed 在 Recall@5、MAP@10 上略好于 clip

  4. 按 answer_type 看

  image_only

  - proposed:
    - Recall@1 0.89
    - Recall@10 1.0
  - clip:
    - Recall@1 0.81
    - Recall@10 1.0
  - ocr:
    - Recall@1 0.0
    - Recall@10 0.03

  这说明纯图像型问题是 proposed 的强项，明显优于 clip，也完全碾压 OCR。

  table_required

  - proposed:
    - Recall@1 0.5283
    - Recall@10 0.975
  - clip:
    - Recall@1 0.5683
    - Recall@10 0.96
  - ocr:
    - Recall@1 0.02
    - Recall@10 0.0967

  这里有个有意思的点：
  - 在 table_required 上，clip 的 Recall@1 略高于 proposed
  - 但 proposed 的 Recall@5/10 更高，整体 MAP 也接近甚至略优
  - 说明表格相关问题里，clip 有时能更快把正确页顶到第一名，但 proposed 整体召回更稳

  这可能意味着：
  - energy 领域某些“表格页”其实具有比较稳定的版式视觉模式，CLIP 很容易匹配
  - proposed 的文本描述虽然更全面，但在个别 top-1 场景不一定总比视觉特征更锐利

  5. 一句话总结

  如果只看 energy：
  - proposed 是最优方法
  - baseline_clip 是很强的次优基线，和 proposed 很接近
  - baseline_ocr 基本不适合 energy 领域

  如果你要把这段写进论文/实验分析里，一个比较自然的表述可以是：

  在 energy 领域，proposed 方法取得了最佳整体检索性能，Recall@10 达到 0.9875，MRR 达到 0.9725，整体优于 baseline_clip；而 baseline_ocr
  在该领域表现显著落后，说明仅依赖 OCR 文本难以覆盖能源文献中大量依赖图表、曲线和视觉结构的信息需求。

  如果你愿意，我可以下一步直接帮你把这段整理成一段论文风格的实验分析文字。


  现在我还想做另外一组实验，让这三个基线在完整的unidoc上进行实验，也就是依然是每个领域作为一个候选集，不过该领域中包含了与样本答案无关的图片


  python -m evaluation.run_unidoc_eval --domain construction --method proposed --top-k 10 --build-index --retry-failed







项目评估模块完整汇报                                                                                                                                                                                                                          
  一、实验体系                                                                                                                                                                                                                                  
  ┌────────────────────────────────────┬──────────────────────┬────────────┬──────────────────┐                           │               实验集               │        数据集        │   候选池   │     完成状态     │                           ├────────────────────────────────────┼──────────────────────┼────────────┼──────────────────┤                           │ COCO 自然图像                      │ MS-COCO val2017 子集 │ 100张      │ ✅               │                           ├────────────────────────────────────┼──────────────────────┼────────────┼──────────────────┤                           │ UniDoc subset（按领域分池）        │ 8个专业文档领域      │ 各领域独立 │ ✅ 全8领域×3方法 │                           ├────────────────────────────────────┼──────────────────────┼────────────┼──────────────────┤                           │ UniDoc crossdomain（跨领域混合池） │ 8领域合并            │ 947张      │ ✅               │                           └────────────────────────────────────┴──────────────────────┴────────────┴──────────────────┘                                                                                                                                                   ---                                                                                                                     二、UniDoc 跨领域实验结果（2026-03-27，800条 query）                                                                                                                                                                                            ┌──────────────────────────────────┬──────────┬──────────┬───────────┬────────┬────────┬──────────────┐                 │               方法               │ Recall@1 │ Recall@5 │ Recall@10 │  MRR   │ mAP@10 │ 延迟中位(ms) │                 ├──────────────────────────────────┼──────────┼──────────┼───────────┼────────┼────────┼──────────────┤                 │ Proposed（MLLM描述+向量检索）    │  0.6726  │  0.9367  │  0.9611   │ 0.9158 │ 0.8989 │     343      │               
  ├──────────────────────────────────┼──────────┼──────────┼───────────┼────────┼────────┼──────────────┤
  │ Baseline-CLIP（跨模态Embedding） │  0.6995  │  0.9413  │  0.9623   │ 0.9371 │ 0.9209 │     282      │
  ├──────────────────────────────────┼──────────┼──────────┼───────────┼────────┼────────┼──────────────┤
  │ Baseline-OCR（OCR文字检索）      │  0.0037  │  0.0107  │  0.0182   │ 0.0081 │ 0.0081 │     511      │
  └──────────────────────────────────┴──────────┴──────────┴───────────┴────────┴────────┴──────────────┘

  关键观察：
  - Proposed 与 CLIP 差距很小：Recall@1 差 2.7pp，MRR差 2.1pp，mAP@10 差 2.2pp
  - 跨领域池下两者 Recall@10 均约 96%，说明 top-10 召回能力接近
  - OCR 在专业文档场景下仍几乎失效（MRR=0.008），与 COCO 实验结论一致
  - Proposed 延迟中位 343ms，CLIP 仅 282ms（无 MLLM 调用开销）

  ---
  三、COCO 自然图像实验结果（2026-03-21，100条 query）

  ┌───────────────┬──────────┬──────────┬───────────┬────────┐
  │     方法      │ Recall@1 │ Recall@5 │ Recall@10 │  MRR   │
  ├───────────────┼──────────┼──────────┼───────────┼────────┤
  │ Proposed      │  0.8100  │  0.9800  │  1.0000   │ 0.8844 │
  ├───────────────┼──────────┼──────────┼───────────┼────────┤
  │ Baseline-CLIP │  0.9500  │  0.9800  │  1.0000   │ 0.9667 │
  ├───────────────┼──────────┼──────────┼───────────┼────────┤
  │ Baseline-OCR  │  0.0100  │  0.0500  │  0.1000   │ 0.0293 │
  └───────────────┴──────────┴──────────┴───────────┴────────┘

  ---
  ###四、横向对比与结论

  ┌─────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────┐   
  │      维度       │                                             结论                                              │   
  ├─────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤   
  │ 整体排名        │ CLIP ≈ Proposed >> OCR                                                                        │   
  ├─────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤   
  │ Proposed vs     │ crossdomain 差距约 2-3pp，远小于 COCO 实验的 14pp，说明专业文档场景下 MLLM                    │   
  │ CLIP            │ 语义描述的优势更明显                                                                          │   
  ├─────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤   
  │ OCR             │ 两个数据集均接近失效，不适合无文字或图表类图像检索                                            │   
  ├─────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤   
  │ 候选池规模影响  │ COCO(100张)→CLIP 领先更大；UniDoc(947张跨领域)→差距收窄，符合预期                             │   
  ├─────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤   
  │ 延迟            │ CLIP 最快(282ms)，Proposed 次之(343ms)，OCR 最慢(511ms)                                       │   
  └─────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────┘   

  ---
  五、工程状态

  所有实验脚本均已稳定：
  - run_unidoc_eval.py：subset 按领域实验，随时可重跑
  - run_unidoc_full_eval.py：crossdomain 实验，MLLM 缓存已全量生成，再次运行无需重复调用 API
  - 历史缓存复用逻辑已修复（_identity_matches 忽略 domain/dataset_name，dataset_name 默认值已改为
  UniDoc-Bench-crossdomain）








  第一步：生成答案（三种方法 + no_rag 基线）                                                             
  # proposed 方法                                                                                     
  python -m evaluation.run_unidoc_gen --method proposed --top-k 5 --output-dir
  data/rag_outputs/unidoc_gen

  # CLIP baseline
  python -m evaluation.run_unidoc_gen --method baseline_clip --top-k 5 --output-dir
  data/rag_outputs/unidoc_gen

  # OCR baseline
  python -m evaluation.run_unidoc_gen --method baseline_ocr --top-k 5 --output-dir
  data/rag_outputs/unidoc_gen

  # 无检索基线（对照组）
  python -m evaluation.run_unidoc_gen --method no_rag --output-dir data/rag_outputs/unidoc_gen        

  输出文件分别为：
  - data/rag_outputs/unidoc_gen/unidoc_gen_proposed_top5.json
  - data/rag_outputs/unidoc_gen/unidoc_gen_baseline_clip_top5.json
  - data/rag_outputs/unidoc_gen/unidoc_gen_baseline_ocr_top5.json
  - data/rag_outputs/unidoc_gen/unidoc_gen_no_rag_top5.json

  ---
  第二步：LLM 评分（对每个生成结果评分）

  # proposed 方法评分
  python -m evaluation.run_unidoc_score \
    --input  data/rag_outputs/unidoc_gen/unidoc_gen_proposed_top5.json \
    --output data/rag_outputs/unidoc_gen/unidoc_gen_proposed_top5_scored.json \
    --resume

  # CLIP baseline 评分
  python -m evaluation.run_unidoc_score \
    --input  data/rag_outputs/unidoc_gen/unidoc_gen_baseline_clip_top5.json \
    --output data/rag_outputs/unidoc_gen/unidoc_gen_baseline_clip_top5_scored.json \
    --resume

  # OCR baseline 评分
  python -m evaluation.run_unidoc_score \
    --input  data/rag_outputs/unidoc_gen/unidoc_gen_baseline_ocr_top5.json \
    --output data/rag_outputs/unidoc_gen/unidoc_gen_baseline_ocr_top5_scored.json \
    --resume

  # no_rag 基线评分
  python -m evaluation.run_unidoc_score \
    --input  data/rag_outputs/unidoc_gen/unidoc_gen_no_rag_top5.json \
    --output data/rag_outputs/unidoc_gen/unidoc_gen_no_rag_top5_scored.json \
    --resume

  评分结果和汇总分别保存为 *_scored.json 和 *_scored.summary.json。

  ---
  说明：
  - --resume 支持断点续传，中断后重跑不会重复计费
  - 若只想评测部分领域，生成时可加 --domains finance academic
  - 命令需在项目根目录（E:/BiShe/Code2/MyProject）执行




 运行命令：

  cd E:/BiShe/Code2/MyProject

  # 推荐先只跑 Phase1，确认 input.jsonl 大小再上传
  python -m evaluation.run_unidoc_gen_batch --method proposed --phase 1 --top-k 5

  # 确认大小后再跑 Phase2（上传+等待）
  python -m evaluation.run_unidoc_gen_batch --method proposed --phase 2

  # 批次完成后解析结果
  python -m evaluation.run_unidoc_gen_batch --method proposed --phase 3

  其他方法同理，把 --method proposed 换成 baseline_clip / baseline_ocr / no_rag 即可。



run_unidoc_gen_batch.py 当前状态：
  - Phase1：构造 input_000.jsonl、input_001.jsonl...（每批次 ≤480MB，每行 ≤6MB，超限自动降级到        
  1024px/q75）
  - Phase2：用 threading.Thread 并行提交所有批次，断点续传通过 batch_id_{idx:03d}.txt 实现
  - Phase3：合并所有 result_*.jsonl，生成与 run_unidoc_score.py 兼容的最终 JSON

  运行方式：

  # 在项目根目录执行 —— 一次性跑完三个阶段
  cd E:/BiShe/Code2/MyProject
  python -m evaluation.run_unidoc_gen_batch --method proposed --top-k 5

  # 或分阶段执行：
  python -m evaluation.run_unidoc_gen_batch --method proposed --top-k 5 --phase 1  # 构造JSONL        
  python -m evaluation.run_unidoc_gen_batch --method proposed --top-k 5 --phase 2  # 提交&轮询        
  python -m evaluation.run_unidoc_gen_batch --method proposed --top-k 5 --phase 3  # 解析结果

  # 最后打分：
  python -m evaluation.run_unidoc_score --input
  data/rag_outputs/unidoc_gen_batch/proposed_results.json

  建议先单独跑 Phase1 验证分批切分是否正常（不需要调用 API），确认无误后再跑 Phase2（需要百炼
  API，每30秒轮询一次直到完成）。