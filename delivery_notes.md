# 论文交付说明

## 已读取的关键材料

- `docs/东北大学本科生毕业设计（论文）书写印制规范.docx`
- `docs/东北大学本科生毕业设计（论文）模版.docx`
- `docs/开题报告_0112.docx`
- `docs/张琪-中期报告-v2.docx`
- `docs/开发技术调研报告.docx`
- `docs/系统分析报告.docx`
- `docs/系统设计报告.docx`
- `backend/`、`frontend/` 下的核心源码
- `evaluation/` 下的评测脚本、方法实现和指标定义
- `实验数据/` 下的检索、生成和微调结果文件

## 事实抽取方式

- 先以源码和实验脚本作为第一手依据，确认系统模块、检索流程、问答流程、文档解析流程和评测流程。
- 再从 `实验数据` 中抽取可回溯的真实指标和结果结论。
- 最后用开题、中期、系统分析、系统设计和技术调研文档补充研究背景、需求演进和设计动机。

## 多份报告的整合方式

- 不直接拼接开题、中期和设计报告原文。
- 对其中仍与最终实现一致的内容进行重写。
- 对仅属于阶段计划、未在代码中落地的内容不写成“已实现”。

## 格式与术语核对方式

- 依据 `facts_inventory.md`、`terminology_table.md`、`compliance_checklist.md` 和 `final_review_checklist.md` 统一全文。
- 以学校书写规范为格式基准，以学校模板为最终载体。

## 当前文件位置

- 中间 Markdown 文件位于项目根目录 `E:\BiShe\Code2\MyProject`
- 最终论文 DOCX 将基于 `docs/东北大学本科生毕业设计（论文）模版.docx` 生成并保存在项目目录内

## 当前建议收尾顺序

1. 固定后端、worker 和前端的启动命令，并验证一次完整演示流程。
2. 保留本次实现后的关键接口与测试结果，作为论文附录和答辩问答依据。
3. 不再继续扩展新功能，转向论文润色、截图整理和答辩准备。

## 本轮后端实现可直接写入论文/答辩的点

- 文档知识库从同步上传升级为“上传入库 + 队列驱动解析 + 进度查询”的异步处理机制。
- 后端新增任务模型、任务查询接口和独立 worker，解决长文档解析阻塞问题。
- 系统新增健康检查、依赖状态检查和指标接口，提升了运行可观测性。
- 图片与文档引入内容哈希、逻辑资源 ID、版本号和最新版本标记，实现去重与版本管理。
- 聊天模块新增回答反馈接口，可沉淀错误样本，为后续评测与优化提供依据。
- 文档检索链补充了查询改写、BM25、RRF 融合和重排序，缩小了图片检索与文档检索能力差距。

## 论文第 5 章建议补写/改写的实现点

- 在“文档知识库实现”中补写异步任务队列、进度管理和 worker 消费机制。
- 在“检索功能实现”中补写文档检索链从纯向量检索升级为混合检索与重排。
- 在“管理与稳定性实现”中补写健康检查、指标接口和任务中心。
- 在“图片知识库实现”与“文档知识库实现”中补写去重和版本管理设计。

## 建议保留的验收证据

- `/api/health/ready` 返回结果截图
- `/api/jobs` 或文档进度页截图
- 文档解析前后结果截图
- 版本历史接口返回结果截图
- 聊天回答与反馈接口截图
- 后端定向测试通过记录

## 本地演示前检查清单

- 后端已启动，地址为 `http://localhost:9090`
- worker 已启动，文档任务不再停留在 `queued`
- 前端已启动，页面能访问文档知识库、图片知识库、检索和聊天页面
- `.env` 中模型配置有效
- 准备好 1 份 PDF 和 1 张图片作为答辩演示样例
- 至少提前跑过一次定向测试

## 本轮已验证的定向测试

```bash
E:/Pyenvironment/multimodal-rag/python.exe -m pytest \
  backend/tests/test_jobs_feedback_health.py \
  backend/tests/test_search_routes.py \
  backend/tests/test_chat_stream_routes.py \
  backend/tests/test_knowledge_base_routes.py -q
```

当前结果：`12 passed`
