from __future__ import annotations

from pathlib import Path

from pptx import Presentation


PPT_PATH = Path(r"E:\BiShe\Code2\MyProject\docs\张琪-中期汇报.pptx")

NOTES = [
    """各位老师好，我是张琪。我的毕业设计题目是“基于语义描述桥接的多模态RAG系统设计与实现”。这次中期汇报我主要从六个方面展开，分别是课题背景、前一阶段工作、系统当前实现情况、实验结果、模型微调探索，以及目前存在的问题和下一阶段计划。""",
    """这一页是本次汇报的整体结构。前半部分我会先说明为什么选择这条技术路线，以及前一阶段具体做了哪些工作；中间部分重点汇报系统目前已经完成到什么程度；后半部分汇报实验和微调的阶段性结果，最后说明当前不足和后续安排。""",
    """本课题的出发点是，多模态知识越来越多，仅靠传统文本RAG难以直接利用图像、图表和复杂页面中的语义信息。针对这个问题，我对两条路线做了比较。一条是直接做统一跨模态向量检索，另一条是先用多模态大模型把图像或文档内容转成结构化文本，再走成熟的文本检索和RAG链路。综合可解释性、工程复杂度和论文论证需求，我最终选择了第二条，也就是语义描述桥接方案，同时保留 CLIP 类方法作为强基线。""",
    """前一阶段的工作可以概括为四部分。第一是文献和技术调研，明确了主方案和基线方案。第二是系统设计，完成了总体架构、模块划分和数据模型设计。第三是原型开发，已经把图片知识库、文档知识库、检索和问答功能串成了闭环。第四是实验准备，已经搭建起检索、生成和微调分析所需的脚本、图表和材料。总体上，中期目标中的“形成可运行原型并开展阶段性验证”已经基本完成。""",
    """这一页展示的是系统架构和代码的对应关系。系统从下到上分为存储层、语义化层、检索层、应用层和展示层。存储层使用 SQLite、ChromaDB 和本地文件系统；语义化层负责图像描述生成、文档解析和查询改写；检索层负责向量检索、混合检索和重排序；应用层负责知识管理、会话管理和RAG编排；展示层对应前端四个页面。也就是说，现在不仅有设计图，而且这些模块在代码里已经有实际落地。""",
    """从当前实现情况来看，系统已经具备较完整的功能闭环。后端方面，支持图片上传后自动生成描述并入库，也支持 PDF 文档上传、后台解析、进度查询和结果查看；同时支持文本检图、以图搜图，以及带历史会话的 RAG 问答。前端方面，知识库、检索和聊天页面都已经完成联调，可以形成比较稳定的演示流程。此外，我还集成了查询改写、多查询扩展、混合检索、重排序和上下文压缩等检索增强能力。""",
    """这一页展示系统界面。左上是图片知识库页面，主要完成图片上传、描述查看和元数据管理；右上是文档知识库页面，支持 PDF 上传和解析结果查看；左下是图文检索服务页面，用于文本检图和以图搜图；右下是 RAG 问答页面，支持多轮问答、会话切换以及引用来源展示。这四个页面基本覆盖了系统从数据接入到最终问答输出的核心使用路径。""",
    """这一页总结系统的核心功能和优化点。第一，系统具备图片语义化能力，把图像内容转换为可检索的结构化文本。第二，具备文档解析能力，可以把 PDF 中的文本片段和页面图像统一纳入知识库。第三，具备双路检索能力，把文本检图和以图搜图统一到同一条检索链路。第四，具备会话化RAG能力，可以保存历史消息和来源快照。第五，我还在此基础上加入了查询改写、混合检索、重排序和上下文压缩，使系统更接近可研究、可扩展的原型，而不只是一个简单Demo。""",
    """这一页汇报检索实验结果。这里我按要求去掉了 OCR 异常基线，只保留 proposed 和 baseline_clip 的对比。从结果看，proposed 在 CRM 和 Energy 领域已经接近甚至超过 baseline_clip，例如 CRM 的 Recall@10 是 0.9768，高于 baseline_clip 的 0.9472；Energy 的 mAP@10 也略优。另一方面，在 Finance 和 Cross-domain 场景下，baseline_clip 仍然更强，说明当前方法在复杂表格页和跨领域混合检索上还有提升空间。延迟方面，两种方法都大致在 500 到 580 毫秒区间。""",
    """这一页是生成实验，也就是 RAG 问答质量的阶段性结果。同样，这里不展示 OCR 基线。从总体指标看，proposed 的答案正确性是 0.86，与 baseline_clip 的 0.87 基本接近，但显著优于 no_rag 的 0.24。这说明检索增强本身是有效的。同时，proposed 的图像上下文相关性达到 0.9286，说明它在检索到与问题匹配的视觉证据方面具有优势。也就是说，虽然在最终答案正确性上和强基线非常接近，但在证据匹配与可解释性方面，本文方法有自己的特点。""",
    """这一页汇报模型微调探索。当前我基于 UniDoc-Bench 图像和描述文本构建了监督微调数据集，其中训练样本 888 条，验证样本 47 条，并基于 Qwen3.5-4B 做了 LoRA 微调，总训练 140 个 step，耗时大约 5 小时 20 分。训练过程中，step-75 的验证损失最低，为 0.5892。结合学习曲线和阶段性人工分析来看，微调后在部分复杂页面上的格式遵循、关键词补充和局部细节描述能力有所提升，但整体效果还没有稳定超过 base 模型，所以这部分我把它定位为可行性验证和后续优化方向。""",
    """虽然系统已经具备完整原型，但目前还有几个比较明确的问题。第一，文档解析在复杂 PDF、表格和跨页场景下还不够稳定。第二，检索增强和外部模型调用叠加后，整体响应时间还有优化空间。第三，在 Finance 和 Cross-domain 场景下，面对强基线时仍有差距。第四，微调结果还没有完全收敛。另外，OCR 基线目前存在异常，所以我不会把它作为本次中期结论的正式依据，后续会单独修复。""",
    """下一阶段的工作重点主要有五项。第一，修复 OCR 基线并补跑相关实验，保证对照实验完整。第二，继续优化图像描述和微调策略，提升小模型在复杂页面上的表现。第三，继续完善文档知识库和多模态 RAG 链路，重点提升表格页、长文档和跨页场景的效果。第四，补充测试和异常处理，打磨一个更稳定的演示版本。第五，同步推进论文正文和答辩材料整理，使系统结果能够顺利转化为最终论文和答辩内容。""",
    """我的汇报到这里结束。整体来看，中期阶段我已经完成了系统原型搭建、核心链路联通以及阶段性实验验证。接下来会继续围绕实验完善、系统优化和论文写作三个方向推进。请各位老师批评指正。""",
]


def write_notes() -> None:
    prs = Presentation(str(PPT_PATH))
    if len(prs.slides) != len(NOTES):
        raise ValueError(f"slide count {len(prs.slides)} does not match notes count {len(NOTES)}")

    for slide, note_text in zip(prs.slides, NOTES):
        notes_slide = slide.notes_slide
        notes_shape = None
        for shape in notes_slide.shapes:
            if hasattr(shape, "is_placeholder") and shape.is_placeholder:
                placeholder = shape.placeholder_format
                if placeholder.idx == 3:
                    notes_shape = shape
                    break
        if notes_shape is None:
            raise RuntimeError("notes placeholder not found")

        text_frame = notes_shape.text_frame
        text_frame.clear()
        text_frame.text = note_text

    prs.save(str(PPT_PATH))
    print(f"notes written: {PPT_PATH}")


if __name__ == "__main__":
    write_notes()
