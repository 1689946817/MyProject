# 当前项目升级计划

## 维护约定

- 本文件用于持续沉淀 `MyProject` 的升级分析、路线图和后续方案答复。
- 后续围绕“项目升级、架构演进、功能重构、工程化优化”的回答，继续补充到本文件。

## 一、与 `pc_multimodal_rag` 的对比结论

### 1. 当前项目已有优势

- 后端边界更清晰。当前项目通过统一适配层编排检索、向量库和 RAG 流程，核心入口集中在 `backend/app/langchain_integration/adapters.py`。
- 离线评测能力更完整。当前项目有独立 `evaluation/` 目录，已经具备基线方法、指标计算和结果落盘能力。
- 项目结构更收敛。后端、前端、评测三块职责明确，较适合继续做“增量优化”，不适合推倒重来。

### 2. `pc_multimodal_rag` 相较于当前项目的主要优点

#### 2.1 知识库建模更完整

- 对方项目显式建模了 `KnowledgeBase -> File -> Chunk` 三层结构。
- 当前项目只有 `ImageRecord` 和 `DocumentRecord`，缺少知识库归属、文件管理和 chunk 级上下文关联。

#### 2.2 知识库管理能力更强

- 对方项目支持知识库列表、文件列表、删除文件、删除知识库、查看 chunk 上下文。
- 当前项目目前更偏“上传文档并查看解析结果”，管理能力不足。

#### 2.3 入库流程的事务与回滚意识更强

- 对方项目上传失败时会执行 SQL 清理和回滚。
- 当前项目 `process_pdf_upload()` 中存在逐步提交和图片处理异常静默吞掉的问题，容易出现部分成功、部分失败但状态仍为成功。

#### 2.4 前后端契约更明确

- 对方项目有更完整的 API 规范文档和更丰富的前端类型定义。
- 当前项目响应模型较轻，检索结果的引用信息、预览信息、结构化字段都偏少。

#### 2.5 前端更产品化

- 对方项目已拆成搜索栏、结果卡片、预览面板、空态、QA 模式等组件。
- 当前项目前端仍偏功能页面组合，管理页感较强，搜索工作台体验不足。

#### 2.6 运维和交付资产更齐

- 对方项目具备监控、备份、Docker/Compose、子模块 README、接口文档等配套。
- 当前项目研发主线明确，但运维、部署、恢复、展示类资产偏弱。

## 二、当前项目的升级原则

### 1. 保留当前项目的正确部分

- 保留现有适配层，不直接照搬对方项目的大型主服务。
- 保留离线评测体系，把它当作后续所有升级的回归基线。
- 继续采用“后端收敛、前端逐步产品化”的策略，不做整体重写。

### 2. 升级目标

- 从“单文档/全局图片记录系统”升级为“可管理的多知识库多文件多 chunk 检索系统”。
- 从“能跑通流程”升级为“具备事务、引用、管理、监控和展示能力”的可演示系统。
- 从“功能堆叠”升级为“架构清晰、接口稳定、可持续迭代”的毕业设计级系统。

## 三、当前项目升级路线图

### Phase 0：基线固化与现状清理

#### 目标

- 在动核心结构之前，先固定当前行为，避免升级过程中失去对照基线。

#### 任务

- 补齐当前接口、数据流、向量库使用方式的结构说明。
- 梳理当前 `backend/app/` 中 API、adapter、vectorstore、parser 的调用链。
- 把现有离线评测命令、后端冒烟命令、前端手工验证流程写入文档。
- 为现有关键模块补充最少量单元测试和集成测试入口。

#### 交付物

- `docs/当前架构说明.md`
- `docs/接口现状说明.md`
- 测试执行说明

#### 验收标准

- 能明确回答“上传 PDF 后，文本、图片、向量、数据库记录分别流向哪里”。
- 当前项目能稳定执行既有评测脚本与后端冒烟测试。

### Phase 1：升级数据模型为多知识库结构

#### 目标

- 让当前项目具备知识库、文件、chunk 的正式数据关系，为后续管理与检索升级打底。

#### 核心设计

- 新增 `KnowledgeBase`
- 新增 `KBFile` 或 `DocumentFile`
- 新增 `DocumentChunk`
- 现有 `DocumentRecord` 可逐步演进为文件层记录，`ImageRecord` 变为文件派生资源或附属资源

#### 任务

- 在 `backend/app/data/` 下新增知识库相关 ORM 模型。
- 给每个文档记录绑定 `knowledge_base_id`。
- 给 chunk 增加 `file_id/doc_id/page_start/page_end/chunk_index/metadata`。
- 明确图片与文档关系，避免仅靠 `extra_metadata.contains(doc_id)` 进行模糊关联。

#### 交付物

- 新数据模型
- 数据迁移脚本或初始化脚本
- 新的 schema 定义

#### 验收标准

- 可以创建知识库。
- 上传文件时能明确落入某个知识库。
- 可以按知识库列出文件和 chunk 数。

### Phase 2：重构入库流程与事务机制

#### 目标

- 解决当前上传链路中提交粒度过碎、错误吞掉、状态不一致的问题。

#### 核心思路

- 从 adapter 中抽出独立 ingestion service。
- 将“文件保存、PDF 解析、chunk 入库、图片抽取、图片描述生成、向量写入、状态更新”变成可控流水线。

#### 任务

- 新建 `backend/app/application/services/ingestion_service.py` 或同类模块。
- 为文档上传引入明确状态机：`Pending -> Processing -> Completed / Failed / PartialFailed`。
- 去掉逐图 `commit` 和裸 `except: pass`。
- 对文本向量入库、图片向量入库、数据库提交定义统一回滚策略。
- 记录失败原因、失败阶段和重试建议。

#### 交付物

- 独立入库服务
- 更完整的错误日志字段
- 文档处理状态模型

#### 验收标准

- 任一阶段失败时可定位失败步骤。
- 不再出现“部分失败但整体 Completed”的静默错误。
- 文档处理可区分完全成功、部分成功和失败。

### Phase 3：升级检索结果与引用链路

#### 目标

- 让当前项目从“返回若干结果”升级为“返回可追溯、可引用、可预览的结果”。

#### 任务

- 扩展检索返回模型，新增：
  - `chunk_id`
  - `doc_id`
  - `file_name`
  - `knowledge_base_name`
  - `page_start/page_end`
  - `citation_number`
  - `preview_url`
  - `thumbnail_url`
  - `structured_data`
- 新增 chunk 上下文接口，支持返回前后若干 chunk。
- 让 RAG 输出携带引用来源，而不是只返回答案和粗粒度结果列表。
- 为文档检索引入过滤能力：
  - 按知识库过滤
  - 按文件过滤
  - 按页码或标签过滤

#### 交付物

- 新响应 schema
- chunk context API
- 带引用的 RAG 返回结构

#### 验收标准

- 前端能展示“答案引用自哪个文件、哪一页、哪一段”。
- 可以点击结果查看上下文而不是只看孤立 chunk。

### Phase 4：前端重构为搜索工作台

#### 目标

- 从当前的功能页面组合，升级为统一的“知识库检索与问答工作台”。

#### 重构方向

- 保留 Vue 技术栈，不必为了追赶对方项目切 React。
- 重点做组件化和交互层升级，而不是框架迁移。

#### 建议组件

- `SearchBar`
- `KnowledgeBaseSelector`
- `ResultList`
- `ResultCard`
- `PreviewPanel`
- `CitationPanel`
- `UploadPanel`
- `EmptyState`
- `ProcessingStatusBadge`

#### 任务

- 将 `Search.vue` 和 `DocumentKB.vue` 拆成组件。
- 新增知识库筛选和文件筛选。
- 检索结果支持选中态、高亮、预览联动。
- 文档结果页支持：
  - chunk 列表
  - 图片预览
  - 页码定位
  - 引用跳转
- RAG 页面与检索页面共享结果预览能力。

#### 交付物

- 新版前端信息架构
- 组件化页面
- 更稳定的 API 类型层

#### 验收标准

- 用户可以在同一工作流内完成“选知识库 -> 搜索 -> 预览 -> 追问 -> 查看引用”。
- 不再需要在多个孤立页面间频繁跳转。

### Phase 5：补齐知识库管理能力

#### 目标

- 让系统具备真正的知识库运维和日常使用能力。

#### 任务

- 新增 API：
  - 创建知识库
  - 获取知识库列表
  - 获取知识库下文件列表
  - 删除文件
  - 删除知识库
  - 查看文件详情
  - 查看 chunk 上下文
- 增加知识库统计信息：
  - 文件数
  - chunk 数
  - 图片数
  - 最后更新时间
- 增加上传任务历史与失败记录。

#### 交付物

- 管理 API
- 前端知识库管理页
- 文件与任务状态页

#### 验收标准

- 管理员可以独立维护多个知识库。
- 文件生命周期可见、可删、可追溯。

### Phase 6：工程化、运维与交付强化

#### 目标

- 让项目从“开发状态”升级为“可答辩、可展示、可恢复”的系统。

#### 任务

- 补 `API_SPEC.md`
- 补部署文档与环境变量说明
- 增加 Dockerfile / docker-compose
- 增加备份脚本：
  - SQL 数据
  - 向量索引元信息
  - 存储目录
- 增加基础监控：
  - 请求耗时
  - 错误率
  - 上传任务统计
  - 检索耗时
- 梳理日志格式，区分 info/warn/error

#### 交付物

- `API_SPEC.md`
- `docker-compose.yml`
- `scripts/backup_*`
- `scripts/restore_*`
- 简单监控或统计接口

#### 验收标准

- 新同学按文档可在本地启动项目。
- 数据可备份恢复。
- 系统运行状态可观察。

### Phase 7：将离线评测接入持续升级闭环

#### 目标

- 保持当前项目“评测能力强”的优势，不让后续重构只停留在结构层面。

#### 任务

- 每次检索链路改动后运行 `evaluation/run_offline_eval.py`。
- 固定一套基线数据集和指标输出目录。
- 为不同版本保存：
  - Recall@K
  - MRR
  - mAP
  - latency
- 将“升级前 vs 升级后”的结果写入实验文档。

#### 交付物

- 版本化评测结果
- 升级前后对比表

#### 验收标准

- 每次升级都能量化收益，而不是只靠主观体验。

## 四、建议的实施优先级

### P0：必须先做

- Phase 1 数据模型升级
- Phase 2 入库事务重构
- Phase 3 检索结果与引用增强

### P1：高价值

- Phase 4 前端工作台重构
- Phase 5 知识库管理能力补齐

### P2：答辩与交付强化

- Phase 6 工程化、运维与部署
- Phase 7 评测闭环固化

## 五、建议的迭代方式

### 迭代 1

- 完成多知识库模型
- 跑通知识库创建、文件入库、文件列表

### 迭代 2

- 完成事务化文档入库
- 完成 chunk 级检索结果与上下文接口

### 迭代 3

- 完成前端搜索工作台重构
- 完成引用与预览联动

### 迭代 4

- 完成知识库管理能力
- 完成备份、监控、接口文档

### 迭代 5

- 完成评测对比与最终展示材料整理

## 六、下一步最建议直接落地的事项

### 首选动作

- 先设计并落地新的数据库模型与后端 API 草案。

### 原因

- 这是后续入库事务、管理能力、前端工作台、引用链路的共同基础。
- 如果底层仍是“全局 document/image record”，后面的优化都会变成临时补丁。

## 七、后续可继续补充的内容

- 数据库表设计草案
- 后端 API 升级清单
- 前端页面与组件树
- 任务拆分甘特图
- 风险点与回退方案

## 八、按周执行的任务拆分表

### 第 1 周：现状固化与设计冻结

#### 本周目标

- 冻结当前主流程。
- 明确升级后的目标数据模型和接口边界。

#### 后端任务

- 梳理当前上传链路：
  - `/api/docs/upload`
  - `LangChainAdapter.process_pdf_upload()`
  - `parse_pdf()`
  - `document_vector_store.upsert_chunks()`
- 梳理当前搜索链路：
  - `/api/search/text-to-image`
  - `/api/rag/chat`
- 形成旧数据模型与新数据模型对照表。

#### 前端任务

- 梳理现有页面：
  - `Search.vue`
  - `KnowledgeBase.vue`
  - `DocumentKB.vue`
  - `Chat.vue`
- 输出未来页面信息架构草图。

#### 文档与测试任务

- 记录现有接口入参与返回。
- 固定一组手工验收样例：
  - 上传 1 个 PDF
  - 查看解析结果
  - 执行 1 次文本检索
  - 执行 1 次 RAG 问答

#### 本周产出

- 升级前接口清单
- 升级后数据模型草图
- 前端页面拆分草图

### 第 2 周：数据库模型落地

#### 本周目标

- 完成多知识库核心表设计并落地 ORM。

#### 后端任务

- 新增表：
  - `knowledge_bases`
  - `kb_files`
  - `document_chunks`
  - `processing_jobs`
- 调整旧表：
  - `document_records`
  - `image_records`
- 给现有记录补 `knowledge_base_id` 或兼容字段。

#### 配套任务

- 更新 schema。
- 补数据库初始化脚本。
- 设计旧数据兼容策略。

#### 本周产出

- 新 ORM 模型
- 初始化或迁移脚本
- 模型关系图

### 第 3 周：知识库管理 API 第一批

#### 本周目标

- 先把知识库管理能力跑通，不改复杂入库逻辑。

#### API 范围

- `POST /api/kbs`
- `GET /api/kbs`
- `GET /api/kbs/{kb_id}`
- `GET /api/kbs/{kb_id}/files`
- `DELETE /api/kbs/{kb_id}`
- `DELETE /api/files/{file_id}`

#### 本周产出

- 知识库 CRUD 第一批接口
- Swagger 可见的 schema
- 后端单元测试

### 第 4 周：文档入库服务重构

#### 本周目标

- 将 PDF 入库从适配层剥离为独立服务。

#### 后端任务

- 新建 ingestion service。
- 将以下动作串成服务流水线：
  - 保存文件
  - 创建 job
  - 解析 PDF
  - 写文件记录
  - 写 chunk 记录
  - 写文本向量
  - 写图片记录
  - 写图片语义向量
  - 更新状态
- 去除静默异常。

#### 本周产出

- `ingestion_service`
- 任务状态模型
- 失败信息结构

### 第 5 周：检索结果结构升级

#### 本周目标

- 让搜索和问答结果带上真正可用的引用信息。

#### 后端任务

- 扩展搜索响应字段。
- 新增 chunk context API。
- RAG 返回结构新增 sources/citations。

#### 前端任务

- 先补最小化 UI 支撑：
  - 结果高亮
  - 引用信息显示
  - 基础预览区

#### 本周产出

- 新检索 schema
- 新问答 schema
- chunk context 接口

### 第 6 周：前端工作台改版

#### 本周目标

- 把“多个功能页”整合成统一搜索工作台。

#### 前端任务

- 页面合并：
  - 搜索区
  - 结果区
  - 预览区
  - 追问区
- 组件化：
  - `SearchBar`
  - `KnowledgeBaseSelector`
  - `ResultCard`
  - `PreviewPanel`
  - `CitationPanel`
  - `UploadDrawer`

#### 本周产出

- 新版主页面
- 统一检索工作流

### 第 7 周：运维与展示能力补齐

#### 本周目标

- 补齐答辩和展示所需的工程资产。

#### 任务

- 增加 `API_SPEC.md`
- 增加备份脚本
- 增加监控统计接口
- 增加部署文档
- 增加 Docker 方案

#### 本周产出

- 部署说明
- 备份恢复说明
- 监控接口

### 第 8 周：回归评测与答辩材料整理

#### 本周目标

- 用量化结果证明升级有效。

#### 任务

- 固定升级前后评测数据。
- 输出指标对照表。
- 补系统架构图、流程图、功能对比图。

#### 本周产出

- 指标对比表
- 升级总结
- 答辩展示材料

## 九、数据库表设计草案

### 1. `knowledge_bases`

#### 作用

- 表示一个逻辑知识库。

#### 建议字段

- `id: str`
- `name: str`
- `description: str | null`
- `status: str`
- `created_at: datetime`
- `updated_at: datetime`

### 2. `kb_files`

#### 作用

- 表示知识库中的原始文件。

#### 建议字段

- `id: str`
- `knowledge_base_id: str`
- `file_name: str`
- `file_type: str`
- `file_path: str`
- `file_size: int`
- `parse_status: str`
- `chunk_count: int`
- `image_count: int`
- `source_type: str`
- `extra_metadata: json/text`
- `created_at: datetime`
- `updated_at: datetime`

### 3. `document_chunks`

#### 作用

- 表示可检索的文本片段。

#### 建议字段

- `id: str`
- `file_id: str`
- `knowledge_base_id: str`
- `chunk_index: int`
- `content: text`
- `page_start: int | null`
- `page_end: int | null`
- `token_count: int | null`
- `content_type: str`
- `extra_metadata: json/text`
- `created_at: datetime`

### 4. `image_assets`

#### 作用

- 表示从文档中提取的图像或独立上传图像。

#### 建议字段

- `id: str`
- `file_id: str | null`
- `knowledge_base_id: str | null`
- `image_type: str`
- `file_path: str`
- `page_index: int | null`
- `generated_description: text | null`
- `status: str`
- `extra_metadata: json/text`
- `created_at: datetime`

### 5. `processing_jobs`

#### 作用

- 跟踪上传与解析任务。

#### 建议字段

- `id: str`
- `knowledge_base_id: str`
- `file_id: str | null`
- `job_type: str`
- `status: str`
- `stage: str`
- `error_message: text | null`
- `started_at: datetime | null`
- `finished_at: datetime | null`
- `created_at: datetime`

### 6. 与现有表的关系建议

- `document_records` 建议逐步被 `kb_files` 替代。
- `image_records` 建议逐步被 `image_assets` 替代。
- 若担心一次迁移风险过高，可先保留旧表，使用新表为主、旧表为兼容读取层。

## 十、后端 API 升级清单

### 1. 知识库管理

- `POST /api/kbs`
  - 创建知识库
- `GET /api/kbs`
  - 获取知识库列表
- `GET /api/kbs/{kb_id}`
  - 获取知识库详情
- `DELETE /api/kbs/{kb_id}`
  - 删除知识库

### 2. 文件管理

- `POST /api/kbs/{kb_id}/files/upload`
  - 上传 PDF 到指定知识库
- `GET /api/kbs/{kb_id}/files`
  - 获取知识库文件列表
- `GET /api/files/{file_id}`
  - 获取文件详情
- `DELETE /api/files/{file_id}`
  - 删除文件

### 3. 解析结果与上下文

- `GET /api/files/{file_id}/chunks`
  - 获取文件 chunk 列表
- `GET /api/chunks/{chunk_id}/context`
  - 获取 chunk 上下文
- `GET /api/files/{file_id}/images`
  - 获取抽取图像列表
- `GET /api/files/{file_id}/parse-result`
  - 聚合返回解析结果

### 4. 检索

- `POST /api/search/text`
  - 文本检索
- `POST /api/search/image`
  - 图搜图
- `POST /api/search/hybrid`
  - 预留混合检索

### 5. 问答

- `POST /api/rag/chat`
  - 当前接口保留，但响应结构升级
- `POST /api/rag/ask-file`
  - 对指定文件问答
- `POST /api/rag/ask-kb`
  - 对指定知识库问答

### 6. 任务与运维

- `GET /api/jobs`
  - 获取任务列表
- `GET /api/jobs/{job_id}`
  - 获取任务详情
- `GET /api/stats`
  - 基础统计
- `GET /api/health`
  - 健康检查

## 十一、核心接口响应结构建议

### 1. 文件上传响应

```json
{
  "success": true,
  "job_id": "job_xxx",
  "file": {
    "id": "file_xxx",
    "knowledge_base_id": "kb_xxx",
    "file_name": "demo.pdf",
    "parse_status": "Processing"
  },
  "message": "文件已接收，正在解析"
}
```

### 2. 搜索结果响应

```json
{
  "query": "XXX",
  "knowledge_base_id": "kb_xxx",
  "results": [
    {
      "chunk_id": "chunk_xxx",
      "file_id": "file_xxx",
      "file_name": "demo.pdf",
      "page_start": 3,
      "page_end": 4,
      "score": 0.91,
      "snippet": "......",
      "citation_number": 1,
      "preview_url": "/api/files/file_xxx/preview?page=3"
    }
  ],
  "total": 10
}
```

### 3. RAG 响应

```json
{
  "answer": "......",
  "sources": [
    {
      "citation_number": 1,
      "chunk_id": "chunk_xxx",
      "file_id": "file_xxx",
      "file_name": "demo.pdf",
      "page_start": 3,
      "page_end": 4,
      "snippet": "......"
    }
  ]
}
```

## 十二、前端页面与组件树建议

### 1. 页面层

- `/workspace`
  - 统一检索与问答工作台
- `/knowledge-bases`
  - 知识库列表与管理
- `/knowledge-bases/:id/files`
  - 知识库文件管理
- `/jobs`
  - 上传和解析任务
- `/settings`
  - 可选，环境配置与统计

### 2. 工作台页面结构

- `WorkspacePage`
  - `WorkspaceHeader`
  - `KnowledgeBaseSelector`
  - `SearchToolbar`
  - `ResultList`
  - `PreviewPanel`
  - `AskPanel`
  - `CitationDrawer`

### 3. 文件管理页面结构

- `KnowledgeBaseFilesPage`
  - `UploadPanel`
  - `FileTable`
  - `ParseStatusBadge`
  - `FileDetailDrawer`
  - `ChunkList`
  - `ImageAssetGrid`

### 4. 状态管理建议

- 若继续保持当前规模，可先使用组合式 API + 局部 store。
- 不建议第一阶段就引入复杂全局状态库。
- 优先抽离：
  - `useKnowledgeBase`
  - `useFileUpload`
  - `useSearch`
  - `useRagChat`
  - `useJobPolling`

## 十三、里程碑与验收口径

### Milestone A：模型改造完成

#### 判定标准

- 可以创建多个知识库。
- 文件可以明确归属到知识库。
- chunk 可以通过 SQL 查到，而不是只存在向量库。

### Milestone B：上传链路稳定

#### 判定标准

- 上传失败可追踪。
- 解析状态可见。
- 不再依赖静默吞错。

### Milestone C：检索结果可引用

#### 判定标准

- 检索结果具备页码、文件名、chunk 上下文。
- RAG 输出具备引用来源。

### Milestone D：前端工作台可演示

#### 判定标准

- 用户可完成完整工作流：
  - 选知识库
  - 上传文档
  - 搜索
  - 预览
  - 追问
  - 查看引用

### Milestone E：工程化闭环完成

#### 判定标准

- 可按文档部署。
- 可备份恢复。
- 可输出评测对比。

## 十四、主要风险与控制方案

### 1. 旧数据兼容风险

#### 风险

- 旧表和新表并存阶段，读写逻辑容易混乱。

#### 控制方案

- 采用“双层兼容”：
  - 新写入走新表
  - 旧查询短期兼容旧表
  - 新页面优先读新表

### 2. 上传链路重构风险

#### 风险

- 一次性改太多，容易导致现有流程完全不可用。

#### 控制方案

- 先保留原 `/api/docs/upload`，内部切到新 service。
- 新 service 稳定后再扩展为知识库上传接口。

### 3. 前端改版过大风险

#### 风险

- 页面一次性全改，容易影响演示节奏。

#### 控制方案

- 先新增 `/workspace`，不立即删除旧页面。
- 新旧页面并行一段时间。

### 4. 评测指标回退风险

#### 风险

- 结构变好了，但检索效果下降。

#### 控制方案

- 每个核心迭代结束后都跑离线评测。
- 指标回退时优先回查 chunk 切分和向量写入逻辑。

## 十五、建议先开工的具体任务单

### Task 1：数据模型设计稿

- 输出最终表结构。
- 明确主键、外键、索引和兼容字段。

### Task 2：后端 schema 重构

- 新增 KnowledgeBase/File/Chunk/Job 的 Pydantic schema。

### Task 3：知识库管理 API 第一版

- 创建知识库
- 获取知识库列表
- 获取知识库文件列表

### Task 4：文档上传改造第一版

- 让上传接口支持指定 `knowledge_base_id`。
- 文件入库后生成 `job_id` 与 `file_id`。

### Task 5：前端最小工作台页面

- 新建 `/workspace`
- 接知识库选择 + 搜索列表 + 预览区

## 十六、如果下一步继续细化，优先补哪些内容

- 数据库 ER 图
- 每个 API 的 request/response 详细字段定义
- 后端目录重构建议
- 前端组件树对应的接口依赖关系
- 首批要改的具体文件清单

## 十七、首批要修改的具体文件清单

### A. 建议优先修改的现有后端文件

#### 1. `backend/app/data/database.py`

##### 现状

- 当前只负责基础 SQLAlchemy engine 和 session。

##### 建议修改

- 保持基础职责不变。
- 如后续引入迁移工具，可在此处补充数据库初始化入口的统一封装。
- 不建议在这里堆业务逻辑。

##### 修改级别

- 低

#### 2. `backend/app/data/doc_models.py`

##### 现状

- 当前只有 `DocumentRecord`，字段主要面向单文档处理状态。

##### 建议修改

- 短期保留该模型用于兼容旧逻辑。
- 中期将其字段迁移到新的 `kb_files` 模型。
- 若不立即废弃，可增加：
  - `knowledge_base_id`
  - `parse_status`
  - `error_message`
- 但更推荐直接新建模型文件承接后续演进。

##### 修改级别

- 中

#### 3. `backend/app/data/models.py`

##### 现状

- 当前以 `ImageRecord` 表示上传图像与 PDF 提取图像，职责混杂。

##### 建议修改

- 短期保留兼容。
- 中期拆为独立 `image_assets` 模型。
- 如果暂时不拆表，至少要补：
  - `knowledge_base_id`
  - `file_id`
  - `page_index`
  - `image_type`

##### 修改级别

- 中

#### 4. `backend/app/application/schemas.py`

##### 现状

- 当前 schema 偏轻，难以支撑引用、预览、知识库管理。

##### 建议修改

- 新增 schema：
  - `KnowledgeBaseOut`
  - `KnowledgeBaseCreate`
  - `KBFileOut`
  - `KBFileDetailOut`
  - `ChunkOut`
  - `ChunkContextOut`
  - `ProcessingJobOut`
- 扩展现有 schema：
  - `SearchResultItem`
  - `ChatResponse`
  - `DocumentRecordOut`
  - `DocParseResult`

##### 修改级别

- 高

#### 5. `backend/app/api/routers/docs.py`

##### 现状

- 当前接口只支持 PDF 上传、文档列表、查看解析结果。

##### 建议修改

- 第一阶段先兼容旧接口不删。
- 将 `/upload` 改为支持传入 `knowledge_base_id`。
- 新增：
  - `/files/{file_id}`
  - `/files/{file_id}/chunks`
  - `/files/{file_id}/parse-result`
  - `/chunks/{chunk_id}/context`
- 逐步从 `doc_id` 概念切到 `file_id` 概念。

##### 修改级别

- 高

#### 6. `backend/app/api/routers/kb.py`

##### 现状

- 当前更像“图片上传接口”，不是真正的 knowledge base router。

##### 建议修改

- 不建议继续沿用当前语义。
- 应重构为真正的知识库管理路由：
  - `POST /api/kbs`
  - `GET /api/kbs`
  - `GET /api/kbs/{kb_id}`
  - `GET /api/kbs/{kb_id}/files`
  - `DELETE /api/kbs/{kb_id}`
- 图片上传相关逻辑应迁入文件或资源路由。

##### 修改级别

- 高

#### 7. `backend/app/api/routers/search.py`

##### 现状

- 当前只返回轻量结果。

##### 建议修改

- 增加知识库筛选参数。
- 增加文件筛选参数。
- 返回 chunk 级结果而不是纯图片级描述结果。
- 为后续 hybrid 检索预留统一接口结构。

##### 修改级别

- 高

#### 8. `backend/app/api/routers/chat.py`

##### 现状

- 当前 RAG 结果返回 `answer + results`，引用结构不明确。

##### 建议修改

- 返回结构改为：
  - `answer`
  - `sources`
  - `citations`
  - `used_knowledge_base_id`
- 增加“限定某个知识库/某个文件问答”的能力。

##### 修改级别

- 高

#### 9. `backend/app/langchain_integration/adapters.py`

##### 现状

- 适配层同时承担上传、解析、DB 写入、向量写入、图片生成、RAG 编排。

##### 建议修改

- 保留 adapter 作为外观层。
- 将以下逻辑迁出：
  - `process_pdf_upload()`
  - 图片提取与描述生成
  - DB 状态更新
- adapter 最终只保留：
  - 调度 ingestion service
  - 调度 retrieval service
  - 调度 rag service

##### 修改级别

- 极高

#### 10. `backend/app/main.py`

##### 现状

- 当前注册了 `kb/search/chat/docs` 四组路由。

##### 建议修改

- 后续应按领域注册：
  - `knowledge_bases`
  - `files`
  - `search`
  - `rag`
  - `jobs`
  - `health`
- 同时补更清晰的 API 标题和版本说明。

##### 修改级别

- 中

### B. 建议新增的后端文件

#### 1. `backend/app/data/kb_models.py`

##### 作用

- 放置 `KnowledgeBase`、`KBFile`、`DocumentChunk`、`ImageAsset`、`ProcessingJob` ORM 模型。

#### 2. `backend/app/application/services/ingestion_service.py`

##### 作用

- 负责文档上传、解析、入库、状态更新、错误处理。

#### 3. `backend/app/application/services/knowledge_base_service.py`

##### 作用

- 负责知识库创建、删除、列表、统计。

#### 4. `backend/app/application/services/file_service.py`

##### 作用

- 负责文件详情、文件列表、文件删除、解析结果聚合。

#### 5. `backend/app/application/services/chunk_service.py`

##### 作用

- 负责 chunk 查询、上下文获取、chunk 详情。

#### 6. `backend/app/application/services/job_service.py`

##### 作用

- 负责任务记录、状态轮询、失败追踪。

#### 7. `backend/app/api/routers/jobs.py`

##### 作用

- 提供任务列表和任务详情接口。

#### 8. `backend/app/api/routers/files.py`

##### 作用

- 承接文件详情、解析结果、chunk、图片资源等接口。

#### 9. `backend/app/api/routers/knowledge_bases.py`

##### 作用

- 承接真正的知识库管理接口。

#### 10. `backend/app/application/repositories/`

##### 作用

- 若后续复杂度继续上升，可增加 repository 层，隔离 ORM 查询逻辑。

### C. 建议优先修改的现有前端文件

#### 1. `frontend/src/router/index.ts`

##### 建议修改

- 逐步从：
  - `/kb`
  - `/search`
  - `/chat`
  - `/docs`
- 演进为：
  - `/workspace`
  - `/knowledge-bases`
  - `/knowledge-bases/:id/files`
  - `/jobs`

##### 修改级别

- 高

#### 2. `frontend/src/App.vue`

##### 建议修改

- 左侧菜单改为新信息架构。
- 主布局保留，但入口导航应围绕“工作台 + 知识库 + 任务”而不是当前功能散页。

##### 修改级别

- 中

#### 3. `frontend/src/views/KnowledgeBase.vue`

##### 现状

- 当前是“图片上传页”。

##### 建议修改

- 不建议继续叫 `KnowledgeBase.vue`。
- 未来可拆为：
  - `KnowledgeBaseListPage.vue`
  - `KnowledgeBaseFilesPage.vue`
- 当前页面内容可以迁移到独立资源上传页或删除。

##### 修改级别

- 高

#### 4. `frontend/src/views/DocumentKB.vue`

##### 现状

- 当前最接近未来文件管理页。

##### 建议修改

- 保留为文件管理页的雏形。
- 后续重点增强：
  - 支持选择知识库
  - 支持文件列表
  - 支持 job 状态
  - 支持 chunk 上下文
  - 支持页码定位

##### 修改级别

- 高

#### 5. `frontend/src/views/Search.vue`

##### 现状

- 当前是文本搜图、图搜图双栏页面。

##### 建议修改

- 逐步重构为 `WorkspacePage.vue`。
- 增加：
  - 知识库筛选
  - 结果列表
  - 引用面板
  - 预览区
- 当前页面的搜索逻辑可部分保留，但 UI 结构应重做。

##### 修改级别

- 极高

#### 6. `frontend/src/views/Chat.vue`

##### 建议修改

- 不建议完全独立存在。
- 中期应并入统一工作台的“追问面板”。

##### 修改级别

- 中

#### 7. `frontend/src/api/kb.ts`

##### 建议修改

- 将当前图片上传相关 API 从“kb”语义中拆出去。
- 后续 `kb.ts` 应只保留真正的知识库管理接口。

##### 修改级别

- 高

#### 8. `frontend/src/api/docs.ts`

##### 建议修改

- 改成面向 `files` 资源，而不是旧 `docs`。
- 增加：
  - `getFileDetail`
  - `getFileChunks`
  - `getChunkContext`
  - `deleteFile`

##### 修改级别

- 高

#### 9. `frontend/src/api/search.ts`

##### 建议修改

- 请求参数增加：
  - `knowledgeBaseId`
  - `fileIds`
  - `filters`
- 响应结构改为 chunk 级结果。

##### 修改级别

- 高

#### 10. `frontend/src/api/chat.ts`

##### 建议修改

- 返回结构改为 `answer + sources + citations`。
- 支持按知识库、按文件问答。

##### 修改级别

- 高

### D. 建议新增的前端文件

#### 页面文件

- `frontend/src/views/WorkspacePage.vue`
- `frontend/src/views/KnowledgeBaseListPage.vue`
- `frontend/src/views/KnowledgeBaseFilesPage.vue`
- `frontend/src/views/JobsPage.vue`

#### API 文件

- `frontend/src/api/files.ts`
- `frontend/src/api/jobs.ts`
- `frontend/src/api/knowledgeBases.ts`

#### 组件文件

- `frontend/src/components/workspace/KnowledgeBaseSelector.vue`
- `frontend/src/components/workspace/SearchToolbar.vue`
- `frontend/src/components/workspace/ResultList.vue`
- `frontend/src/components/workspace/ResultCard.vue`
- `frontend/src/components/workspace/PreviewPanel.vue`
- `frontend/src/components/workspace/CitationPanel.vue`
- `frontend/src/components/files/FileTable.vue`
- `frontend/src/components/files/FileDetailDrawer.vue`
- `frontend/src/components/files/ChunkList.vue`
- `frontend/src/components/files/ImageAssetGrid.vue`
- `frontend/src/components/jobs/JobTable.vue`

## 十八、建议的代码提交批次

### Commit 1：数据模型与 schema

#### 目标

- 不动前端页面，先完成后端数据结构升级准备。

#### 变更范围

- `backend/app/data/*`
- `backend/app/application/schemas.py`

### Commit 2：知识库管理 API

#### 目标

- 先做知识库 CRUD 和文件列表。

#### 变更范围

- `backend/app/api/routers/knowledge_bases.py`
- `backend/app/api/routers/files.py`
- `backend/app/main.py`

### Commit 3：入库服务重构

#### 目标

- 把上传和解析逻辑迁到 service。

#### 变更范围

- `backend/app/langchain_integration/adapters.py`
- `backend/app/application/services/ingestion_service.py`
- `backend/app/api/routers/docs.py`

### Commit 4：检索与问答结构升级

#### 目标

- 让检索和 RAG 返回引用信息。

#### 变更范围

- `backend/app/api/routers/search.py`
- `backend/app/api/routers/chat.py`
- `frontend/src/api/search.ts`
- `frontend/src/api/chat.ts`

### Commit 5：前端工作台第一版

#### 目标

- 新增 `/workspace` 页面，不立刻删除旧页。

#### 变更范围

- `frontend/src/router/index.ts`
- `frontend/src/App.vue`
- `frontend/src/views/WorkspacePage.vue`
- 新增 workspace 组件

### Commit 6：知识库管理页面与任务页

#### 目标

- 完成新工作流的外围管理页面。

#### 变更范围

- `frontend/src/views/KnowledgeBaseListPage.vue`
- `frontend/src/views/KnowledgeBaseFilesPage.vue`
- `frontend/src/views/JobsPage.vue`

## 十九、首批实施时建议直接修改的文件顺序

### 第一批

- `backend/app/data/kb_models.py`
- `backend/app/application/schemas.py`
- `backend/app/api/routers/knowledge_bases.py`
- `backend/app/main.py`

### 第二批

- `backend/app/application/services/ingestion_service.py`
- `backend/app/api/routers/docs.py`
- `backend/app/langchain_integration/adapters.py`

### 第三批

- `backend/app/api/routers/search.py`
- `backend/app/api/routers/chat.py`
- `frontend/src/api/search.ts`
- `frontend/src/api/chat.ts`

### 第四批

- `frontend/src/router/index.ts`
- `frontend/src/App.vue`
- `frontend/src/views/WorkspacePage.vue`
- `frontend/src/components/workspace/*`

## 二十、如果继续往下细化，下一步最有价值的内容

- 直接输出 `kb_models.py` 的表结构草稿
- 直接输出 `application/schemas.py` 的新 schema 草稿
- 直接列出知识库 API 的 request/response 字段
- 直接给出 `/workspace` 页面组件层级图
